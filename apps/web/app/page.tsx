/**
 * PKG-00 SCOPE: toolchain boot proof only. The minimum proof UI
 * (human/AI distinction, Decision creation, Burst protection —
 * 14 §44 PHASE 11) is not implemented yet.
 */
export default function Home() {
  return (
    <main>
      <h1>nquiry — Phase 0 skeleton</h1>
      <p>Repository and toolchain skeleton (PKG-00). No proof UI exists yet.</p>
    </main>
  );
}
