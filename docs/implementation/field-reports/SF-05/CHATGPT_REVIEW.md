# SF-05 — adversarial second reading
Reviewer stance: "prove the repairs are roots, not paint; prove nothing semantic moved"

| Claim | Challenge | Answer |
|---|---|---|
| Workspaces are normalized | "The runtime never returned duplicate ids — did you fix a defect that does not exist?" | The human saw duplicates because seven scenario runs founded seven same-named Workspaces. Doc 26 §7 demands id-normalization AND that a human cannot believe one Workspace exists twice; both are materialized: merge by id (proven with repeated records in L3) and canonical disambiguation of homonyms (founded time · id8). Nothing is hidden, nothing collapsed. |
| Geometry uses the field | "Two rings is a hack for fourteen; what about forty?" | Ring 2 takes the rest; when even two rings cannot fit, the decision falls to the constellation, now 3–4 capsules per row. Recorded as an observation (§7 of FIELD_REVIEW). |
| The organ is not a dashboard | "Chambers now have borders — cards again?" | ≤ 1 px at α 0.07, translucent fill, no outer shadow, contour brackets, one organ surface, glyph/marker/tone per class. The SF-04 law was re-bound explicitly, not silently. |
| Authority ≠ role | "`roleRelation()` maps Owner → 'Owner role' — is that an authority claim?" | It is a membership mark with the word "role"; authority is only ever the server's binding (chain). The gate test forbids role comparisons as gates and passes. |
| Commit event | "The event is derived from client state — a client truth?" | It derives from the lifecycle after the server's re-read succeeded and describes what the page requested; it is not read by anything and disappears; proof persists in the chamber. |
| Login | "Hover changes the DOM — HOVER != STATE?" | `data-attract` is projection-local attention; no request, no verdict; the loading core stays quiet; the same law as the encounter cascade. |
| Breadcrumb | "Ellipsis hides route meaning" | Titles keep every segment inspectable; the current segment keeps ≥ 22 characters; status words remain in the accessibility tree; links unchanged. |
| Decision Surface | "You wrapped it — is that 'symbiotic' or lipstick?" | The container renders the same components in the field frame with chambers and the NON_PROOF core; no data changed; the F02 attack tests (no action element in main for boundaries) still pass. Recorded for the human: this is the PKG-29 prototype; F07 re-homes it. |
| Performance | "Breathing auras on every node — large repaint?" | Transform on small absolutely-positioned auras; measured (perf-ab variants I/J/K). |

Open observations for the human: see FIELD_REVIEW.md §7.
