# SF-05 — WU-26.1 … WU-26.9 root repairs (doc 26 §48)
FIELD: NQUIRY Symbiotic Frontend Human Review Repair · every unit: MUST BECOME TRUE / MUST REMAIN IMPOSSIBLE / FALSIFIER / RED → ROOT REPAIR → LOCAL PROOF → INTEGRATION → REGRESSION → RECONSTRUCTION

| WU | Authoritative home / root repair | Must become true | Must remain impossible | Falsifier (doc 26) | Local proof |
|---|---|---|---|---|---|
| 26.1 Workspace normalization | `lib/field/workspaces.ts` `normalizeWorkspaces()` (new producer): merge by `workspaceId` (first-seen order, relation count), detect homonymous names, attach the canonical distinguisher "founded <UTC time> · <id8>"; the page consumes normalized identities only | one canonical id → one primary node; same-named different Workspaces distinguishable by canonical facts | dedup by name, by position, by reference; hiding a legitimate Workspace | §41.1–4, 9–10 | `tests/field/workspaces.test.ts` 7/7; L3 sf05 "one canonical id → one primary node" (three records → one node; 7 homonym pairs → 14 nodes with 14 distinct distinguishers), "two Workspaces with the same name are never collapsed" |
| 26.2 Workspace geometry | `segmentWorkspaces()` ring segmentation (founding + first five on the inner containment ring, the rest sm on the outer ring — geometric, no invented category); stack constellation capsules 196 px whose `li` sizes to its membrane (`width: max-content`), three per row in the 650 px desktop column, four on tablets (no waterfall) | field space used; deterministic positions; no overlap; no overflow | duplicate entries feeding layout; a single vertical list where space exists | §41.3–8 | L3 sf05 "many Workspaces use the field" (two rings, ≥ 4 columns, 0 overlaps, positions equal on reload, nothing below the field box), narrow width; H states 57–60 (wide/desktop/tablet/phone) |
| 26.3 organ foundation | `components/field/chambers.tsx` (new grammar: `ChamberHead`+glyph, `AuthorityRelation`, `ParticipationRoster`, `ProvenanceSpine`, `Identifiers`/`Token`, `BoundaryMark`+`boundaryClassOf`, `roleRelation`); `Plane` carries `data-semantic`; organ v2 CSS (solid surfaces, bounded contours, tone tokens per class) | one organism of differentiated chambers; every class recognizable in text and structure | identical cards; role read as authority; proof as identifiers | §42.1–5, 7–8 | `tests/field/sf05-primitives.test.tsx` 9/9 |
| 26.4 semantic chambers | pages re-homed: Workspaces (action, identity), Workspace (action, identity, participation roster with role marks, authority), Challenge (action, authority chain at CHALLENGE scope, proof spine, identifiers), Session (question/frozen/action, authority at SESSION scope + grant, participation roster with participant/controller marks + admit, proof spine, identifiers, decision entry); `Unavailable` → boundary classes; `BurstCapturePanel` protected question surface (HUMAN_ONLY seal, focus reciprocity with the human core) + confirmation chamber (what freezes, what cannot happen, cancel/commit); `FrozenQuestionSet` sealed artifacts | ACTION ≠ AUTHORITY ≠ ROLE ≠ MEMBERSHIP ≠ PARTICIPATION ≠ PROOF ≠ IDENTIFIER ≠ BOUNDARY; CHALLENGE scope ≠ SESSION scope | inferring authority from role, participation from membership, controller from Challenge authority (all data stays the server's) | §42, §47.10–15 | L3 sf05 chambers suite (5 tests); every SF-01/F02/F03 test id kept (regression lanes) |
| 26.5 commit resonance | `lib/field/fieldEvent.ts` `deriveFieldEvent()` (event only for committed + re-read done + described relation), `components/field/FieldEvent.tsx` (bloom → hold → collapse as ONE CSS animation; dismissed on `animationend`; reduced motion: motionless step animation of the same length; no timer — HD-11); pages describe events at request time with the exact scope; `EffectOutcome` becomes a quiet proof line when committed + re-read (`data-quiet`) | the field registers a material change after canonical confirmation; proof persists | event before re-read; success for a failed effect; wrong scope (Challenge control described as Session control); permanent event; blocking; generic "Success" | §43.1–10 | `tests/field/fieldEvent.test.ts` 4/4; L3 sf05 commit suite (gated re-read, denied → none, reduced motion); H state 12 check |
| 26.6 login field | `app/login/page.tsx` attention/focus as projection-local attributes (`data-attract`, `data-focus`); `FieldBackground` bounded dense star group (10); CSS: gathered field on attention (dense group 0.35 → 1, action glow, membrane), local focus response, loading core quiet, boundary core | entry into a living field; hover/focus reciprocity; no success before the verdict | success motion before authentication; noise; reduced accessibility | §44 | L3 sf05 login suite (3 tests) + `auth.spec.ts` regression; H states 54–56 |
| 26.7 breadcrumb | `RelationTrace` titles on compressed segments; CSS: one-line field path ≥ 768 px (established segments ellipsis 22ch / 13ch on tablets, current state never compresses, status words hidden on tablets) | one coherent path at desktop widths; centred identity; logout clear | random wrap of the lifecycle state; overlap | §45 | L3 sf05 breadcrumb suite (1280, 1024 long name); H state 66 |
| 26.8 Decision Surface | `SessionViewContainer` renders the field frame (background, rail, trace to the Session, decision core with NON_PROOF, organ chambers context / evidence (burst) / decision); route page delegates; every PKG-28/29 component, test id and text kept; `main` still has no action element outside the record form | symbiotic surface; NON_PROOF explicit; same canonical data | raw text fallback; invented charts/proof | §46 | L3 sf05 decision suite (2) + `session-view.spec.ts` / `decision.spec.ts` regression; H state 68 |
| 26.9 intensification | CSS: node auras 0.40–0.70 at rest, 1 + scale 1.14 on encounter; desynchronized node breathing by OPACITY (`--breath-phase` from the key hash, 9–13.4 s; a scale animation re-rasterized every aura layer, measured +4 ms); no breathing in the compressed constellation; pulses 7 px (9 px resonating) with double glow; base currents thicker; core membrane answers encounters; dense stars on every surface at 0.35; resonating chamber contour brightens | stronger reciprocity, more visible particles, subtle breathing, more alive ambient | flicker, fast motion, large bright areas, layout shift | §10–§14, §37 | L3 sf05 "nodes breathe subtly and out of phase"; perf-ab variants I/J/K |
