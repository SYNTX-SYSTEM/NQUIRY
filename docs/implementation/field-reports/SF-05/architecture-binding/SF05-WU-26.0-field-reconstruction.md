# SF-05 (doc 26 human-review repair) — WU-26.0 FIELD RECONSTRUCTION
FIELD: NQUIRY Symbiotic Frontend Human Review Repair (doc 26, sha256 `14ed130a7b8c5568…`, byte-identical copy in the worktree)
Tree: worktree `frontend-symbiotic`, HEAD `644e1c8` + the uncommitted SF-02/SF-03/SF-04 materialization (kept; nothing reset).
BLUE (F02/F03 backend + published field) and RED (F04) untouched; no commit/tag/push/publish.

## Runtime and evidence reconstructed
- SF-04 review runtime `nquiry-sf04-inspect` on http://127.0.0.1:13400 (persistent review service); harness run-7 = 53/53.
- `GET /workspaces` for the owner and the facilitator returns **14 Workspaces with 14 distinct ids in 7 same-named pairs**
  ("SF-03 Inspection", "Cross-Functional Customer Activation …"), one pair per harness scenario run
  (`setup-scenario.mjs` founds a fresh Workspace each run). The server sends every record once (query:
  "every Workspace the actor holds ACTIVE membership in, oldest first").

## Inverse chains (VISIBLE EFFECT → CONSUMER → RELATION → PRODUCER → AUTHORITY → STATE → ROOT)
| FBR (doc 26 §6) | Visible effect (run-7 screenshot 07) | Consumer | Relation | Producer | Root |
|---|---|---|---|---|---|
| HR-01 duplicate-looking Workspaces | 7× "SF-03 Inspection", 7× the stress name, every node "accessible Workspace" | `app/workspaces/page.tsx` node builder | containment (membership) | `listWorkspaces()` records (distinct ids, homonymous names) | no normalization layer: the page maps records 1:1 to nodes and projects no distinguishing canonical fact (founding time, id) — a human cannot tell the homonyms apart |
| HR-02 waterfall | 15 md nodes cannot fit the containment ring at 700 px → `data-topology="stack"` → capsule constellation wraps 2 per row (`max-width 320px`) into a 1 900 px column | `FieldStage`/`Topology` + stack CSS | geometry | `layoutField` (one ring) | one ring for any count; stack capsules sized for phones, not for a 700 px desktop column |
| HR-03/05/15 sidebar grammar, undifferentiated chambers, forms in a sidebar | organ = header + `Plane` chambers that all render headings + text + forms the same way | `Planes`/`Plane` (SF-04) | context | pages | one chamber grammar for every semantic class; no chamber-level semantics (`data-chamber` = primary/relational/logic only) |
| HR-04 proof as text | `dl.provenance` rows | Session/Challenge/Workspace proof planes | proof | `establishedBy`, ids | no provenance geometry primitive |
| HR-06 `Committed.` boxes | `EffectOutcome` (`data-outcome=committed`) persists in the chamber | `EffectSurface.tsx` | effect lifecycle | `useEffectField` (settled + reconstruction done) | the confirmed effect has no transient field event; the box doubles as the confirmation |
| HR-07 passive login | static access core, ambient background only | `app/login/page.tsx` + `.access-*` CSS | access | — | no attention/focus reciprocity on the access field |
| HR-08 breadcrumb wrap | `.trace ol { flex-wrap: wrap }`; long established labels take the rail's first column; the current lifecycle label wraps below | `RelationTrace` + `.trace` CSS | position | `sessionTrace()` (5 segments) | the trace is a wrapping row of pills, not a path with compression |
| HR-09 Decision Surface raw | `/decision` route renders unstyled components outside the field frame | `SessionViewContainer` + PKG-28/29 components | decision | `fetchSessionView` | the route never entered the field language (no frame, no background, no chambers) |
| HR-10 identifiers | `.mono` with `overflow-wrap: anywhere` splits UUIDs mid-token | proof planes | identifiers | ids | no identifier primitive (controlled breaks, copy) |
| HR-11 confirmation | `div.panel[role=group]` inside the burst panel | `BurstCapturePanel` | irreversible action | `COMPLETE_BURST` capability | generic panel styling |
| HR-12/13/14 hover, ambient, breathing | encounter cascade exists (SF-04) but auras 0.28–0.58, pulses 6 px; 7 particles; nodes do not breathe | `globals.css` | attention/ambient | — | amplitude tuned conservatively in SF-04 |

## Field protection map (doc 26 §9)
Preserved untouched by this repair: core anatomy, orbit geometry engine, lifecycle ring/current/passed/later, relation currents + pulses, encounter attributes, `data-*` contracts, every test id, DOM order, accessibility tree, reduced-motion law, backend reads (`lib/api/*` unchanged).

## Work units (doc 26 §48) → files
| WU | Authoritative home | Files |
|---|---|---|
| 26.1 Workspace normalization | projection producer | `lib/field/workspaces.ts` (new), `tests/field/workspaces.test.ts` (new), `app/workspaces/page.tsx` |
| 26.2 Workspace geometry | field layout | `app/workspaces/page.tsx` (ring segmentation), `globals.css` (stack constellation density on desktop) |
| 26.3 organ foundation | `components/field/topology/FieldStage.tsx` (`Plane` semantic classes), `components/field/chambers.tsx` (new primitives) |
| 26.4 semantic chambers | pages, `BurstCapturePanel`, `FrozenQuestionSet`, `EffectSurface`, CSS |
| 26.5 commit resonance | `components/field/FieldEvent.tsx` (new), `lib/field/fieldEvent.ts` (new), `FieldFrame`, pages |
| 26.6 login field | `app/login/page.tsx`, `FieldBackground`, CSS |
| 26.7 breadcrumb | `RelationTrace`, CSS |
| 26.8 Decision Surface | `app/…/decision/page.tsx`, `SessionViewContainer` + components (class names only), CSS |
| 26.9 intensification | CSS (auras, pulses, breathing, stars) |
| 26.10–12 | pages, lanes, harness `SF-05/browser-evidence/harness`, reports |
