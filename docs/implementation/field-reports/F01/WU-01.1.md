# WORK UNIT REPORT
FIELD: F01 — IDENTITY · WORKSPACE · GOVERNANCE
WORK_UNIT: WU-01.1 (current identity adapter normalization)

## Mission
Confirm the current identity adapter (real local login,
local-login-auth field) is the single, consistent identity-resolution
path across the codebase, with no leftover header-trust remnants —
per 19 §21's own "CURRENT FIELD" line: "local auth → real."

## Files changed
NONE.

## Relation materialized
Existing capability confirmed consistent, not re-materialized.

## Tests written first
N/A — verification-only Work Unit, no new relation.

## Initial failing proof
N/A.

## Implementation
Reconstructed current reality (19 §2 Repository Authority Law) via
direct grep across `packages/`, `apps/api/src/`, `apps/web/lib/`,
`apps/web/components/`, `apps/web/app/` for
`resolve_actor|ACTOR_USER_ID_HEADER|x-nquiry-actor`. Every hit is a
docstring/comment reference explaining the local-login-auth field's
own historical fix (Architecture 17's disclosed header-trust weakness,
GAP-14-001). The only live function matching the old name pattern is
`application.http_dispatch._resolve_actor_from_session`, the real
session-cookie-based resolver — not header-based. No dead code, no
inconsistent second identity path exists anywhere.

`AuthenticatedPrincipal`/`IdentityPort` (`packages/security/identity.py`,
PKG-01) remain the single canonical identity shape; the local
credential adapter (`packages/security/local_auth.py`,
`packages/application/auth_handler.py`) is the one concrete adapter in
use, unchanged since the local-login-auth field's own close.

## L0 Direct Tests
N/A (no code change).

## L1 Owning Module Tests
```
$ pytest -q tests/security/test_local_auth.py tests/e2e/test_auth_handler.py tests/e2e/test_http_auth.py
47 passed
```
(Re-run for currency; identical result to the local-login-auth field's
own close.)

## L2 Relation Tests
N/A.

## L3 Field Integration
N/A — F01 has no integration test suite of its own yet (this is the
first Work Unit).

## L4 Adjacent Field Regression
N/A.

## L5 Static / Architecture Gates
Already re-verified fresh by F00 at this same `HEAD`; not re-run
redundantly for a zero-code-change Work Unit.

## L6 Runtime Smoke
N/A (no code change; F00 already proved real login works against the
real running containers at this `HEAD`).

## L7 Browser Semantic Proof
NOT_APPLICABLE — no human-visible change.

## Recursive DeepSweep
Nothing changed because nothing changed. Confirmed, not assumed.

## Inverse DeepSweep
N/A.

## First Broken Relation
NONE.

## Known limitations
NONE new.

## Unresolved blocker
NONE for this Work Unit specifically. See WU-01.4 for this Field's own
real blocker (HARD-DEP-001).

## Current git status
```
?? apps/web/AGENTS.md
?? apps/web/CLAUDE.md
?? docs/implementation/field-reports/F01/WU-01.1.md
```

## Result
PASS
