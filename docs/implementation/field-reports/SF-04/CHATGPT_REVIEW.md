# SF-04 — adversarial second reading (self-review in the ChatGPT-review format used since SF-01)
FIELD: SF-04 — NQUIRY SYMBIOTIC FIELD ORGANISM · reviewer stance: "prove it is not a dashboard in space, and prove the frontend invented nothing"

## 1. Does the materialization match doc 25, organ by organ?
| Organ (doc 25) | Materialized | Reviewer's residual doubt | Answer |
|---|---|---|---|
| Semantic Core (§6) | aura · 3 rings · membrane · nucleus · micro-orbit; breathing ≤ 2.4 %; directional lean | "is the lean a semantic claim?" | no: `--vec-*` is projection-local (hovered/focused entity); it carries no state, rests at 0 |
| Field entities (§7) | weight/band/tone from rules; mass = padding + aura; deterministic offsets | "mass via padding is subtle — is weight visible at all?" | the aura's opacity (0.28 → 0.58) and the role indicator carry it; text never scales (doc 23 §6.1). Recorded as a review item for the human (visual sufficiency) |
| Relation currents (§8) | curved base + drifting current + endpoints by class | "reciprocal only for participation — is that canonical?" | F03 PARTICIPATION is a typed relation on both sides (admit ↔ take part); recorded in RULE-R's doc comment |
| Context organ (§10) | aside with header/chambers/bridge/tokens | "`.plane`, `data-plane`, `.instruments` still exist — residue?" | kept as contract hooks (SF-01/SF-03 specs, wide two-column law); the visual composition is the organ's; the class names are not visible |
| Traversal trace (§11) | route nodes + membranes + chip | "the chip hides links on phones" | they stay in the accessibility tree; the toggle reveals them visually; SF-01 mobile trace contracts unchanged |
| Atmosphere (§12) | pressure zone + resonance wash inside the field box; SF-03 background beneath | "two atmospheres?" | one medium in two depths: ambient (viewport) and field-local (box); both decorative |
| Encounter (§13) | FieldState → attributes → cascade | "hover writes DOM attributes — is that state?" | view model only; no request, no selection, cleared on leave; identical for keyboard |
| Responsive (§15) | ≥ 1200 / 768–1199 / < 768 / < 390 | "the SF-03 phone breakpoint was 860; the rail keeps 860" | field breakpoints follow doc 25; the rail law (doc 23 §9) is untouched and independent; 768–860 renders the tablet composition (proven at 800) |

## 2. Where could the frontend still invent semantics?
- `relationCount` is passed by pages (e.g. a controller who is also a participant). It is derived from the same
  projection arrays the page renders — not a new fact. Falsifier 1 holds.
- `role` (core-adjacent/primary/secondary/peripheral) is a projection class derived from band + state (RULE-B); it is
  written to `data-role` for review, never read back.
- `tension` exists as a type only. If a future producer projects conflict, RULE-R must be extended explicitly — the
  guard throws today on any attempt.

## 3. Where did SF-04 change an earlier contract, and is each change lawful?
| Change | Earlier law | Why lawful |
|---|---|---|
| phone rows → constellation capsules | doc 23 §14.3 "full-width readable rows" | doc 25 §15.4 supersedes: "responsive redistribution, not stacking"; capability reachability re-proven |
| `<line>` spokes → `<path>` arcs | SF-02 primitives | doc 25 §8 "curved, never harsh"; the `.path` class and `data-path` state law kept |
| planes → chambers | doc 22 §4.8 instruments | doc 25 §10; every instrument's content, headings, ids and test ids unchanged |
| breakpoints | doc 23: 1100 / 860 | doc 25 §15 + the SF-03 threshold weakness (1100–1199 px two columns could not fit) |
| probes (`scrollWidth`) | SF-03 containment heuristic | decorative layers are excluded from the scroll metric; the text-rect law is unchanged and still enforced |

## 4. What the reviewer should look at in the browser (review/FRONTEND_BROWSER_REVIEW.md)
The wide Challenge and Session fields (currents, organ beside the field), the tablet composition (organ below), the
phone constellation with the chip, the encounter cascade by pointer and by keyboard, reduced motion, and the stress
content. Runtime: http://127.0.0.1:13400 (identities in the harness header).

## 5. Open items for the human (not blockers)
1. Visual sufficiency of semantic weight (aura + role indicator + padding; no type scaling by design).
2. The Session field with 13 lifecycle phases on a 1280 px desktop is content-dense; the geometry decides orbit or
   constellation from the rendered frames (doc 23 §7.6) — confirm the constellation is acceptable where it occurs.
3. Doc 25 §17 tokens are added beside the SF-02 tokens (no removal); a later consolidation could alias them.
