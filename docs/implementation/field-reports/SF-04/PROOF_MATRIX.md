# SF-04 PROOF MATRIX — doc 25 §21.11 required falsifiers · mandate visual falsifiers · doc 25 §23 non-negotiables
FIELD: SF-04 — NQUIRY SYMBIOTIC FIELD ORGANISM (materialization of doc 25 on the SF-02/SF-03 tree)
Lanes: L0 vitest (`tests/field/*`) · L3 isolated mocked browser (`tests/e2e/sf04-field.spec.ts` + SF-01/SF-03/F01/F02 regression, desktop 1280 + Pixel 7) · L7 isolated real stack (`scripts/run_sf01_real_stack.sh`) · H review harness on the inspection runtime (`browser-evidence/harness/review-harness.mjs`, states 1–52) · P perf-ab (×4 CPU throttle).
Result columns are filled from the final tree's runs (see FIELD_REVIEW.md for the numbers).

## A. doc 25 §21.11 required falsifiers (the implementation FAILS if any occurs)
| # | Falsifier | Why it cannot occur (mechanism) | Proof | Result |
|---|---|---|---|---|
| 1 | semanticWeight because a node looks better emphasized | RULE-W is a pure function of canonical state + relation density (`projection.ts`); no visual input exists; provenance on the DOM (`data-provenance="RULE-W;…"`) | L0 projection.test "orders weight by canonical state, never by label length"; sf04-primitives "relation density raises the weight … never the label length"; L3 "every entity carries … the rule chain"; H organismChecks | GREEN |
| 2 | reciprocal animation on a non-reciprocal relation | RULE-R yields `reciprocal` only for `participation` (canonically mutual); the stylesheet animates `alternate` only for `[data-relation-type="reciprocal"]` | L0 "participation is reciprocal ONLY because the canonical relation is mutual"; L3 Session "participation is a reciprocal (alternating) current"; H states 49–51 | GREEN |
| 3 | tension rendered without canonical tension | no producer of tension exists; RULE-R never returns `tension` (every family × state tested); `assertProvenance` throws on it; no CSS for it | L0 "tension is never produced" + guard; L3 `g.relation[data-relation-type="tension"]` = 0; H "tension never" | GREEN |
| 4 | responsive layout changes meaning or authority | breakpoints move DOM (same order) between compositions; capabilities/reasons/controls unchanged (SF-03 phone test: every capability reachable; SF-04: constellation keeps every control) | L3 sf03 phone "keeps every capability reachable"; sf04 responsive suite (6 widths + phone + compact); H states 41–46 (entities = server list on every viewport) | GREEN |
| 5 | hover/focus mutates canonical state | the encounter writes only DOM attributes / CSS vars from `ProjectedFieldState`; no request path exists in the handler | L3 "hovering … sends nothing" (no non-GET request, no outcome), "keyboard focus … identical cascade"; H states 47–48 "no request from the encounter" | GREEN |
| 6 | context preview treated as committed | `data-chamber-resonating` is presentation; effects still go through the effect lifecycle with server outcomes (`EffectOutcome`) | L3 sf03 "hover never becomes a commit"; F02/F03 regression specs (outcomes only from committed responses) | GREEN |
| 7 | local state becomes identity/governance/lifecycle/relation truth | `ProjectedFieldState` holds hovered/focused ids + vector only; the route decides the active core; every displayed state is read from projections (`api()` cross-checks in H) | H every state: `kind: "projection"` checks compare the DOM to the server projection read in the same session; `localTruthDetected` flag | GREEN |
| 8 | motion implies a state not produced canonically | every animation is keyed by a DOM attribute derived from a rule (`data-relation-type`, `data-core-state`, `data-resonating`); rest state has no encounter attributes | L3 "motion follows the relation class"; "nothing encountered at rest"; reduced-motion suite | GREEN |
| 9 | visual hierarchy silently redefines canonical hierarchy | bands come from relation families (RULE-B), weight from state; the DOM order is the semantic order (22 §34.1); every visual has textual carriers (`.node-marker`, `.core-state`, organ header text) | L0 sf04-primitives (DOM order, textual state); L3 SF-01 order test; H trace/order checks | GREEN |
| 10 | context organ data accepted as authority because displayed | the organ displays and hosts request surfaces; it has no state attribute; requests still resolve on the server (capabilities decide) | L0 "the organ carries no state of its own"; L3 organ "hosts the request surfaces unchanged"; H "organ carries no state of its own" | GREEN |
| 11 | pixel position used to infer semantic role | role/band are inputs to the geometry, never outputs; `organicOffset` derives from the key, not the position | L0 geometry "weight scales frame/scale not band"; projection tests (band from kind only) | GREEN |
| 12 | atmospheric tone used to infer canonical state | `.field-bg[data-regime]` and `.stage-atmosphere` carry no attribute the app reads; pressure/wash react only to projection-local vectors | L3 atmosphere "inert at rest"; H "atmospheric medium present, inert to the pointer" | GREEN |
| 13 | visualTone creates a new category | RULE-T maps every state to one of the four approved tones; `data-tone` ∈ {living, stable, boundary, neutral} asserted on every node | L0 tone mapping; L3 "every entity carries band, tone …" | GREEN |
| 14 | action button visibility treated as authorization | unchanged SF-02 law: controls exist iff the server capability says so; every effect's outcome comes from the server (F01/F02/F03 regression) | L3 SF-01/F02 regression (81 tests) + real stack | GREEN |
| 15 | reduced motion changes semantic meaning | the global reduced-motion rule removes animation/transition only; every layer, attribute and text stays | L3 "reduced motion: … every layer and every meaning stays"; sf03 reduced-motion depth; H reduced-motion states (2, 46, 51) | GREEN |

## B. Mandate visual falsifiers (§22)
| Falsifier | Mechanism against it | Proof |
|---|---|---|
| a dashboard in space | the right side is one organ (`aside.organ`) with chambers sharing one surface, a membrane header grown from the core, a bridge to the field; no borders/shadows per chamber | L3 organ test (computed styles); H "chambers share the organ surface"; screenshots run-1 |
| a prettier sidebar | the organ attaches beside/below/under the field by viewport class, mirrors field entities (`data-relation-key` ↔ `data-key`), previews on encounter | L3 encounter (chamber preview, token resonance); H states 47–48 |
| animations as decoration | every animation is bound to a relation class, a core state, a route position or an encounter; the list of keyframes is closed (aura/ring/membrane/micro-orbit/current-drift/current-breathe/organ-emerge/rail-energy + SF-03 ambient) | perf-ab variant G/H; L3 reduced motion; PROOF §A-8 |
| motion without relation meaning | directional = drift with direction; reciprocal = alternate; context = breathe; latent = none | L3 "motion follows the relation class"; Session currents |
| invented semantics | projection module is the only source of semantic visuals; provenance on every node/relation; `assertProvenance` in development | L0 projection tests; DOM attributes in H |

## C. doc 25 §23 non-negotiables (1–25) — binding
1 no game UI / 2 no loud neon: palette unchanged (SF-02 tokens), auras ≤ 0.2 alpha · 3 readability: type floors (doc 23 §6.1) kept, containment probes GREEN · 4/23 motion only with semantic purpose (§A-8) · 5 no stacked cards (§B) · 6 no mechanical distribution: deterministic organic offsets · 7 no text overflow: containment probes at 8 viewports · 8 no random jitter: FNV-1a seeded offsets, tests for determinism · 9 particles do not dominate: SF-03 seven faint particles unchanged · 10 no fatigue: breathing 9–18 s, drift 9 s, micro-orbit 48 s, hover 280/420 ms · 11 usability: every control/reason unchanged (regression lanes) · 12 responsiveness ≠ stacking: constellation + membrane sheet + tablet oval · 13 breadcrumb in field language: route nodes/membranes/chip · 14 core is not a logo container: identity + state + meta text in the nucleus, anatomy around it · 15 relation lines are currents · 16–25 = §A-1…15.
