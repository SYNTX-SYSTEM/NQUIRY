NQUIRY FIELD REVIEW
FIELD: SF-03 — NQIRY SYMBIOTIC SURFACE EVOLUTION (doc 23 delta on the SF-02 Field)
HEAD: 644e1c8 + uncommitted SF-02 (baseline, kept) + uncommitted SF-03, branch frontend-symbiotic (worktree /home/codi/Entwicklung/nquiry/worktrees/frontend-symbiotic)
STATUS: READY_FOR_HUMAN_FRONTEND_REVIEW (NOT committed / tagged / pushed / PUBLISHED / FIELD_GREEN)
ARCHITECTURAL AUTHORITY: docs/implementation/frontend/23_NQIRY_SYMBIOTIC_SURFACE_EVOLUTION_ARCHITECTURE.md (delta; §20 falsifiers 1–58), 22 (protected semantics), 21/SF-01, 20 SFE.
HUMAN DECISIONS USED: SFE::MATERIALIZE_APPROVED_ARCHITECTURE_DELTA (2026-09-25).

MISSION: SF-02 (semantically correct Symbiotic Field) → SF-03 (deeper, more spatial, more legible, more atmospheric, more content-aware, more responsive, more coherent, more living) with every protected relation preserved. No projection, command, capability, authority, visibility or lifecycle rule changed.

DELTA → ROOTS (WU-SF03.0): living background → FieldBackground/.field-bg · typography/containment → tokens + max-content frames + unclamped core · content-aware geometry → NEW lib/field/geometry.ts (estimate frames → weighted angles → radius from content → outer-ring offset search + spoke nudge → collision loop vs every frame + core → width-bounded growth into a taller ellipse → compact frames → stack as compacted representation; box height follows content; mode from viewport class; deterministic, SSR-safe) · breathing room → stage grid + ellipse + BREATHE fractions · breadcrumb → RelationTrace as orientation rail (pseudo-element energy, static text) · centred identity → Identity.tsx in a 3-column rail · instrument constellation → .instruments grid (2 cols ≥ 1500 px; active relation first; governance/proof beside) · reciprocity → FieldStage delegated focus/hover → data-active-relation (presentation only, keyboard-equivalent) · surfaces → layout input passed through; all 13 lifecycle labels readable (lifecycleEmphasis replaces label density).

FIRST BROKEN RELATIONS REPAIRED (with roots): TYPE FLOOR → TOKENS; FRAME MAX → CONTENT NEED (sm 192 px; uppercase-aware textWidth); RING MIN → CORE CORNER (soft minimum + collision loop); WIDTH EXHAUSTED → RADIUS (vertical growth, compaction, stack); MEASURED BOX → MODE → BOX loop (mode from viewport class; height need from width alone); COMPACT RULE → STACK (phone rows); TINTED FRAME → OPAQUE BASE (path through text); SPOKE NUDGE → AFTER OFFSET SEARCH; RADIUS STEP → WIDTH BOUND (D-SF03-3); ESTIMATED → RENDERED FRAMES + STORED → DERIVED HEIGHT (D-SF03-4); RENDERED CORE + DECISION ON RENDERED FRAMES (D-SF03-5); AMBIENT COST (30 ms → 16/19 ms rAF at x4: paint area/layer count reduced per doc 23 §5.5).

WHAT CHANGED: new lib/field/geometry.ts, lib/field/reciprocity.ts, components/field/Identity.tsx, tests/field/geometry.test.ts, tests/field/sf03-primitives.test.tsx, tests/e2e/sf03-field.spec.ts (stress content), docs 23 copy, field-reports/SF-03/**. Changed: globals.css (tokens, stage/topology, core, nodes, instruments, rail, identity, trace energy, nebula, reciprocity, floors), FieldStage (client: mode decision, reciprocity, Topology with ResizeObserver + layout context), Orbit (layout-driven px positions, ellipse paths, data-relation, emphasis), FieldBackground (nebula/haze/particles), FieldFrame (rail), login page (centred mark), the four Field pages (layout input, lifecycle emphasis), topology.ts (unit-circle helpers removed), playwright.sf01.config.ts (mobile matches sf03), sf01-field.spec.ts (core centre = topology centre), topology.test.ts (superseded tests removed). NOT changed: apps/api, packages, migrations, lib/api/*, components/f02/*, components/f03/*, effect lifecycle, gates, ledgers, F04.

PROOF (final tree): L0 232/232 · tsc 0 · eslint clean · L3 mocked **102 / 102** (2.0 min; 81 SF-02 regression + 21 SF-03 stress/acceptance; desktop + Pixel 7; lane alone) · L7 isolated real stack **14 / 14** (6.9 min, lane alone, host load 4–6; earlier runs on the same lane at load 9–17: 12/14, 12/14, 13/14 — only the F03 protected spec's 90 s budget, no assertion ever failed) · evidence run-5 **40 / 40** (30 SF-02 regression states + 10 doc 23 acceptance states; axe serious/critical 0, overflow 0, no unexpected HTTP, keyboard focus visible everywhere) (40 states: 30 SF-02 regression + wide Challenge/Session/Workspace/Human-Question, keyboard reciprocity, reduced-motion depth, stress desktop/wide/phone) · perf-ab x4 throttle: 16.3 / 19.2 ms rAF (SF-02: 16.6 / 29). PROOF_MATRIX.md maps doc 23 falsifiers 1–58.

TEST COMMANDS: (cd apps/web && npx eslint . && npx tsc --noEmit && npx vitest run) · (cd apps/web && npx playwright test --config playwright.sf01.config.ts --output=/tmp/sf03-mocked) · bash scripts/run_sf01_real_stack.sh (ALONE) · harness: see review/FRONTEND_BROWSER_REVIEW.md §7.

CEILINGS: visible-identity falsifiers need the human's eyes (run-5 screenshots 32–40, 19); F03 real-stack protected spec 90 s budget marginal on this loaded host (no assertion fails); §9.4 breadcrumb reciprocity optional/not implemented; two-column constellation from 1500 px; evidence PNGs untracked (~33 MB/run).

OPEN VISIBLE ITEMS (Case 2): OV-1 two-column threshold; OV-2 ordinals on later lifecycle labels; OV-3 breadcrumb→Field reciprocity; OV-4 F03 spec budget.

GIT: uncommitted; index empty; changes only under apps/web and docs/implementation. Commit only on explicit human instruction.

REVIEW QUESTION: Do run-5 screenshots 32 (Challenge, wide), 19 (Human Question Field, wide), 35 (Session frozen, wide), 34 (Workspace, wide), 36 (reduced motion), 37–40 (stress) satisfy doc 23 §27: alive, calm, readable, contained, dynamic, breathing, rail belongs, identity centred, constellation not waterfall, governance/authority/privacy/proof intact?
