# WORK UNIT REPORT
FIELD: F03 — PROTECTED HUMAN QUESTION FIELD
WORK_UNIT: WU-03.5 — HD-12 validator and BND-008 BURST_INPUT_VALID

## Materialized
`domain/burst_input.py`: deterministic FORM rule (pinned question-mark table, last non-whitespace character), technical safeguards (2000 chars, NUL, lone surrogates: rejected, never altered). BND-008: capture requires a HUMAN actor (`CAPTURE_REQUIRES_HUMAN_ACTOR`, mirrored in the application guard), an established input check (else REQUIRE `BURST_INPUT_VALID_NOT_ESTABLISHED`), invalid input DENY.
## Ceiling (HD-12)
Form, not meaning: "It is blue?" passes; a real question ending in a closing quote fails. NQ-GAP-023 stays OPEN; no model is consulted.
## RED / GREEN
`test_burst_input.py` 36 (RED: module absent), `test_bnd_008_capture_input.py` 8 (RED: 8 failed). Legacy BND-008 tests and the differential test were adapted (valid input supplied for capture).
