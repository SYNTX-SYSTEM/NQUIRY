# WORK UNIT REPORT
FIELD: F03 — PROTECTED HUMAN QUESTION FIELD
WORK_UNIT: WU-03.3 — FBR-F03-4: fingerprint over immutable birth facts

## First Broken Relation
The PKG-07 fingerprint hashed `question_id:record_version`. `record_version`/`normalized_text` stay updatable after the Burst (03 §25.5, F04), so an untouched raw set could stop verifying (03 TRN-SESS-005: "frozen-set identity must be reconstructable").

## Repair
`domain.burst_membership.compute_frozen_membership_fingerprint` (scheme `NQ-FROZEN-SET-V2`): membership id, question id, SHA-256 of the exact `original_text`, `captured_order`, actor, `captured_at` (UTC); ordered by (order, id). DISCLOSED DEVIATION from 14 §19's historical choice. `application/frozen_set.py` recomputes/verifies from canonical persistence.
## RED / GREEN
`tests/domain/test_burst_membership_fingerprint.py`: 8 failed / 1 passed -> 9 passed. After-normalization verification: `test_complete_burst.py::test_fingerprint_survives_later_normalization_work`.
