# WORK UNIT REPORT
FIELD: F03 — PROTECTED HUMAN QUESTION FIELD
WORK_UNIT: WU-03.4 — FBR-F03-3 and FBR-F03-7: unresolved capture

## First Broken Relations
- FBR-F03-3: a capture attempt had no persisted link to its Burst (`commands` carried no target refs), so "no capture write acknowledged to the user remains unresolved" (TRN-BURST-005) / "capture uncertainty blocks completion" (BND-008) could not be enforced.
- FBR-F03-7 (discovered while repairing FBR-F03-3): a BND-014 DENY left the attempt without an outcome and the idempotency record IN_PROGRESS forever. With targets persisted that would block a Burst's completion forever after a merely stale or de-authorized capture.

## Repair
Migration `f6b2c4d9a318`: `commands.target_refs`. `CommandRepository.list_unresolved_for_target` (an attempt IN_PROGRESS or INDETERMINATE, no COMMITTED attempt, no recovery record resolved). `CommitCoordinator._mark_denied_at_gate`: DENIED outcome + idempotency FAILED_PRECOMMIT.
## RED / GREEN
`tests/e2e/test_f03_unresolved_capture.py`: 6 failed (+ FBR-7 assertion RED) -> 7 passed; full suite 1393 passed / 2 skipped.
