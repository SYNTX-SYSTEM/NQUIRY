# WORK UNIT REPORT
FIELD: F03 — PROTECTED HUMAN QUESTION FIELD
WORK_UNIT: WU-03.1 — FBR-F03-5: PARTICIPATION authority source

## First Broken Relation
Effect gate <-> participation authority. AUTH-DEP-Q-001's source is the participation right; BND-014 admitted only BINDING/ROLE/FOUNDING and `audit_events.authority_source_type` had a closed CHECK. Encoding capture as ROLE/BINDING would fabricate provenance (HD-6).

## MUST BECOME TRUE / MUST REMAIN IMPOSSIBLE / FALSIFIER
- True: BND-014 ALLOWs `ParticipationAuthority(session_id)` for a human with a CURRENT SessionParticipation + ACTIVE membership in the same Workspace; the proof is PARTICIPATION, ref = participation id, scope `SESSION:<id>`.
- Impossible: ALLOW for non-participant / controller / Owner / left participant / revoked member / other Session or Workspace / non-human / missing reader.
- Falsifiers: `tests/e2e/test_participation_authority_source.py` (12).

## RED
Collection ImportError (`ParticipationAuthority` absent). GREEN: 12 passed.

## Repair (authoritative home)
`boundaries/authority_source.py`, `bnd_014_commit.py`, `audit/models.py`, migration `c4e9a2b7d135` (CHECK widened). Composition wires `participations`. Later refactor (WU-03.6): the rule is one function, `boundaries/participation_right.py`, used by BND-014 and the precommit BND-005 participation evaluator.
## Regression
boundaries + command_commit_event + typed provenance + session control: 505 passed.
