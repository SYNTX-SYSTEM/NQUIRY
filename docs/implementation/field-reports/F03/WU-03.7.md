# WORK UNIT REPORT
FIELD: F03 — PROTECTED HUMAN QUESTION FIELD
WORK_UNIT: WU-03.7 — CMD_COMPLETE_BURST (TRN-SESS-005 + TRN-BURST-005)

## Materialized
`application/burst_completion_handler.py`: manual, BINDING at `SESSION:<id>` (HD-9/HD-1), one bundle: Burst COMPLETED + fingerprint, Session QUESTION_CAPTURE. Lock first, re-read, unresolved-capture and empty-set blockers (`complete_burst_blocker`, shared with the projection). Second completion: replay (same key) or `blocked` (new key). No scheduler, no timer path (HD-11), no PAUSED path (HD-10).
## Tests
`test_complete_burst.py` (23): lawful freeze + establishedBy, time passing creates no effect, participant / Owner / Challenge-scope / non-human / other-Workspace denied, stale, zero questions, second completion, late capture (app and DB), unresolved capture blocks until recovery resolves, only committed captures frozen, normalization does not break verification, tamper detection, 7 failure points, INDETERMINATE.
`test_f03_concurrency.py` (4, real two-connection races on a cloned database): capture-vs-completion exclusion both ways, serialized captures, 5-thread race. MUTATION CHECK: removing the row lock fails all 4; restored, all pass.
RED: collection error. GREEN: 23 + 4 passed.
