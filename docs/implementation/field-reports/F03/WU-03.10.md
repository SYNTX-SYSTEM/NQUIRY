# WORK UNIT REPORT
FIELD: F03 — PROTECTED HUMAN QUESTION FIELD
WORK_UNIT: WU-03.10 — Frontend and real-stack proof

## Materialized
`components/f03/` (BurstCapturePanel, BurstTimer, OwnQuestions, FrozenQuestionSet), `lib/burst.ts`, `inquiryClient` (capture/complete), Session page integration. Controls only from server capabilities; text sent byte-exact; one Idempotency-Key per logical capture; explicit two-step completion; timer = elapsed + guidance only.
## Tests
vitest 25 new (123 total), eslint/tsc clean, `next build` OK. Real stack (containers rebuilt from this tree, no network interception): `f03-protected-question-field` (full stakeholder path incl. adversarial in-page fetches) and `f03-accessibility` (axe WCAG A/AA 0 serious/critical, keyboard-only capture and completion, no horizontal overflow), desktop + Pixel 7: 4 passed; whole real-stack lane 10 passed; mocked lane 39 passed.
Two spec-authoring defects were fixed (a hydration race on a control touched right after reload; a locator matching two elements) and one guard hit (a comment naming the forbidden API). None was an application defect.
Screenshots: `visual/`.
