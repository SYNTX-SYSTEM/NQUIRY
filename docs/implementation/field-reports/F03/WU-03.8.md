# WORK UNIT REPORT
FIELD: F03 — PROTECTED HUMAN QUESTION FIELD
WORK_UNIT: WU-03.8 — Read side: HD-13 visibility and capabilities

## Materialized
`session_position` serves `questionSet` filtered ON THE SERVER (ACTIVE: participant own only, controller count only, others nothing; COMPLETED: every member the full frozen set with authors, `verified` fingerprint), `serverNow`, guidance-only timer facts, and `CAPTURE_QUESTION` / `COMPLETE_BURST` capabilities sharing preconditions with the Commands.
`tests/e2e/test_f03_projection.py` (12). RED: 11 failed / 1 passed. GREEN: 12 passed. Other participants' text is asserted absent from the raw payload.
