# WORK UNIT REPORT
FIELD: F03 — PROTECTED HUMAN QUESTION FIELD
WORK_UNIT: WU-03.6 — CMD_CAPTURE_BURST_QUESTION

## Materialized
`application/burst_capture_handler.py`; precommit chain BND-001/002/003 + BND-005 participation + BND-008; effect gate BND-014 PARTICIPATION. Refusal order: denied -> rejected (input) -> blocked (state) -> stale -> BND-008 -> commit. Burst row locked, Session+Burst re-read under the lock. Question and membership ids derived from the command id (a retry cannot create a second). No `origin`/`author`/`mode` parameter exists.
## Tests
`tests/e2e/test_capture_burst_question.py` (39): lawful capture and provenance, byte-exact Unicode, HD-14, before-ACTIVE blocked, non-participant / other Workspace / other Session / non-human denied, form rejection, stale, idempotent retry, changed payload, cross-command key reuse both ways, immutability, failure injection at 7 commit points (no partial capture, retry succeeds once), INDETERMINATE recorded as unresolved and not blindly retried, audit immutability, EC-2.
RED: collection error. GREEN: 39 passed.
