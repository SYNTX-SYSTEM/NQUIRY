# WORK UNIT REPORT
FIELD: F03 — PROTECTED HUMAN QUESTION FIELD
WORK_UNIT: WU-03.9 — HTTP routes

## Materialized
`POST …/burst/questions` (wire contract: `originalText`, `expectedBurstVersion`; any other field is `rejected`, never honoured) and `POST …/transitions/complete-burst`; outcome mapping: rejected 400, denied 403, blocked 422, stale 409, replayed 200.
`tests/e2e/test_http_f03.py` (12): full flow with real cookies, distinct outcomes, forged origin/author/mode rejected, identical retry replays, changed payload and cross-command key rejected, static route inventory (no PUT/PATCH/DELETE on questions), static no-AI-import gate on the handlers.
RED: 11 failed / 1 passed (404). GREEN: 12 passed.
