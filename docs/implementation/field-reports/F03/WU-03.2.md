# WORK UNIT REPORT
FIELD: F03 — PROTECTED HUMAN QUESTION FIELD
WORK_UNIT: WU-03.2 — FBR-F03-2, FBR-F03-6, EC-1: membership law at its persistence home

## First Broken Relations
- FBR-F03-2 freeze <-> concurrent capture: the freeze trigger read the Burst with a plain SELECT and accepted PREPARED Bursts.
- FBR-F03-6: no per-Burst uniqueness of `captured_order`.
- EC-1: one Burst per Session only partly enforced (partial unique index).

## Repair: migration `e5a1b3c8f204`
INSERT requires an ACTIVE Burst (row read `FOR SHARE`); capture-law trigger (04 AUTH-DEP-Q-001): actor holds a CURRENT participation, Question is HUMAN, authored by the actor, of the Session's Challenge; `UNIQUE (burst, captured_order)`; full `UNIQUE (session_id)` on `question_bursts`.
Handler-side lock: `get_by_session_for_update` (`FOR NO KEY UPDATE`), used by capture and completion (WU-03.6/03.7).

## RED / GREEN
`tests/e2e/test_f03_membership_law.py`: 9 failed / 2 passed -> 11 passed.
## Side effects (disclosed)
PKG-07 constraint tests now admit their author as participant (test plumbing); `seed_local_demo.py` starts its Burst and admits the owner (NON_PROOF fixture).
