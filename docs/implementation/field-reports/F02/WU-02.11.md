# WORK UNIT REPORT
FIELD: F02 — CHALLENGE · SESSION · PARTICIPATION
WORK_UNIT: WU-02.11 — Field-end proof: containers from this tree, real-stack GREEN, L8 regression, test-environment isolation, documentation closure

## Mission
Prove the complete Field on the real runtime and run the full affected regression. Repair every failure at its first broken relation. Close the documentation.

## Runtime proof (L6)
`docker compose -p nquiry --profile app up -d --build` from this worktree: api, web and worker rebuilt, postgres healthy. `GET /healthz` 200. The new route `POST /workspaces/x/challenges` without a cookie returns `{"kind":"denied","reasonCode":"NO_VALID_SESSION"}`. CORS preflight allows `Idempotency-Key`. Migration head `b3d8e5f0a2c7` on both `nquiry` and `nquiry_test`.

## Browser proof (L7), REAL STACK against the rebuilt containers
`npx playwright test --config playwright.real-stack.config.ts` → **4 passed**:
- `f02-inquiry-context.real.spec.ts` [desktop], [mobile]: the complete F02 flow with three real identities, including unauthorized-actor denial, stale version, cross-Workspace read + write denial, and malformed input → `rejected`;
- `f02-accessibility.real.spec.ts` [desktop], [mobile]: axe 0 serious/critical, keyboard-only control, no horizontal overflow.

The global setup refuses any spec containing `page.route`/`context.route`/`route.fulfill`/`routeFromHAR` and refuses to run if the stack is not reachable.

## L8 regression (final, this tree)
| Lane | Result |
|---|---|
| Backend live DB (`nquiry_test`, isolated), run 1 | **1342 passed, 2 skipped, 0 failed** |
| Backend live DB (`nquiry_test`), run 2 | **1342 passed, 2 skipped, 0 failed** |
| Backend pure-Python | 745 passed, 599 skipped, 0 failed |
| ruff format / ruff check | 408 files formatted / all checks passed |
| mypy | no issues (166 files) |
| architecture dependency / provider-SDK / test-only-import gates | PASS / PASS / PASS |
| migrations (static + live) | PASS, 22 revisions, single head |
| eslint / tsc | clean / clean |
| vitest | 91 passed |
| Playwright MOCKED component-contract lane | 37 passed |
| `next build` | compiled; 8 routes |
| Real-stack lane (containers) | 4 passed |

## First Broken Relations found in this WU (and repaired at their homes)
1. **Real-stack residue vs. unscoped test assertions.** After the real-stack runs committed durable governed history into the shared `nquiry` DB, 10 PKG-era/F01 tests failed:
   - 5× `test_commit.py`: `SELECT * FROM commit_units … .first() is None`, unscoped;
   - 1× `test_create_workspace.py`: `COUNT(commit_units) == 0`, unscoped;
   - 4× `test_outbox_worker.py`: `OutboxWorker.deliver_due()` scans all due records globally (correct worker semantics), and the tests' static envelope fakes raised `KeyError` on foreign records.

   This is the same class F01 met in WU-01.9 (resolved there by an environment reset).

   Repair:
   - (a) the six unscoped assertions are scoped to the test's own Workspace or compare before/after;
   - (b) the live-DB suite runs on an **isolated `nquiry_test` database** that no runtime writes to (runbook §22). The outbox-worker tests assert a property of the whole outbox and are only meaningful there.

   On the shared DB, after real-stack runs, the 4 outbox-worker tests still fail **by construction** (classified ENVIRONMENT, documented, not hidden).
2. **Intermittent F01 flake** `test_founder_with_two_workspaces_sees_both_oldest_first`: both Workspaces were created at the identical fixed timestamp, and the query ordered by `created_at` only, so the tie order was undefined. Repair: deterministic tie-break `(created_at, workspace_id)` in `list_active_memberships_for_user`, plus distinct creation times in the test. Stable over 6 repeats and 2 full runs.

## Files changed in this WU
- MOD `tests/command_commit_event/test_commit.py` (5 scoped assertions)
- MOD `tests/e2e/test_create_workspace.py` (before/after commit-unit count)
- MOD `packages/persistence/membership_repository.py` (tie-break)
- MOD `tests/e2e/test_accessible_workspaces_query.py` (distinct timestamps)
- MOD `docs/RUNTIME_OPERATION.md` (§18–§22: surface, gaps, HARD-DEP status, stakeholder walkthrough, test lanes + isolated DB)
- MOD `README.md` (F02 state, lanes)
- MOD `apps/web/app/page.tsx` (stale docstring)
- NEW `F02/FIELD_REVIEW.md`, `F02/CHATGPT_REVIEW.txt`, MOD `F02/STATUS.md`

## Recursive DeepSweep / reconstruction chain
- **Changed relation**: test environment ↔ runtime history.
- **Parent**: 20 §12 (real-stack proof writes real history, as it should); F00's command manifest (live-DB pytest now targets `nquiry_test`).
- **Siblings**: F08 (worker loop) will make the global outbox scan real at runtime. Its tests must keep their own DB.
- **Governance / authority / persistence / API / frontend**: unchanged in this WU (tie-break only affects ordering of equal timestamps).
- **Tests**: 6 scoped/deterministic fixes. No test weakened: every repaired assertion still proves the same property for the test's own data.
- **Reconstructed Field**: F02 complete (see FIELD_REVIEW).

## Inverse DeepSweep
See FIELD_REVIEW §Inverse DeepSweep: the complete chain from visible QUESTION_GENERATION to authenticated identity, each link asserted in real-stack and real-DB tests.

## Limitations
The live-DB suite needs the one-time `nquiry_test` setup (runbook §22). Running it on the shared DB after real-stack runs yields 4 ENVIRONMENT failures by construction.

## Mock / Fixture / External ceiling
See FIELD_REVIEW.

## Git state
Uncommitted, index empty, `git diff --check` clean.

## Result
PASS.
