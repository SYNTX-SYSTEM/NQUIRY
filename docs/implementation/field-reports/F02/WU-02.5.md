# WORK UNIT REPORT
FIELD: F02 — CHALLENGE · SESSION · PARTICIPATION
WORK_UNIT: WU-02.5 — Real-stack browser lane + DEV-ONLY identity provisioning (HD-3) + RED; normalized-red test repair

## Mission
Create the persistent REAL-STACK Playwright lane, write the full F02 product flow as a failing browser test before any F02 frontend/API work, and materialize HD-3 (a second/third local human without authority).

## Parent / Current Field
20 §12 (browser proof classes), 19 §22, HD-3 (16 §41 REC-003). F02.

## Relation materialized
- "Real browser → real web → real FastAPI → real local auth → real PostgreSQL" as an automated, repeatable lane (`npm run e2e:real`), guarded against mocking.
- "Dev identity exists" without "membership/role/authority exists" (HD-3).

## Initial state
Only `page.route()`-mocked Playwright suites. One user identity creatable only via the fixture seed script. `test_habb_grant_constraints.py::test_grant_without_active_membership_is_rejected` red in every live-DB run since F00 (unscoped COUNT).

## Target state
Lane exists, fails closed on mocks and on a stack that isn't running; F02 flow RED for the right reason; identity provisioning creates identity only; no normalized red.

## Files changed
- NEW `packages/test_support/dev_identity.py` (test_support: production import forbidden by gate)
- NEW `scripts/dev_provision_local_identity.py` (opt-in env `NQUIRY_DEV_IDENTITY_PROVISIONING=I_UNDERSTAND_THIS_IS_DEV_ONLY`, local DB host only, no HTTP route)
- NEW `tests/security/test_dev_identity_provisioning.py` (9 tests)
- NEW `scripts/run_real_stack_e2e.sh` (postgres → migrations → uvicorn :8000 → next dev :3000 → lane)
- NEW `apps/web/playwright.real-stack.config.ts`, `apps/web/tests/real-stack/{global-setup.ts,identities.ts,f02-inquiry-context.real.spec.ts}`
- MOD `apps/web/package.json` (`e2e:real`)
- MOD `tests/security/test_habb_grant_constraints.py` (assertion scoped to the test's own Workspace)

## Test-first intent
- MUST BECOME TRUE: a second human can log in; F02 flow is expressible end-to-end in a real browser.
- MUST REMAIN IMPOSSIBLE: provisioning creating membership/role/binding; the lane running with any `page.route`/`route.fulfill`/`context.route`/`routeFromHAR`; the lane "passing" against a stack that isn't running.
- FALSIFIERS: rows in memberships/roles/bindings for a provisioned user; guard bypass without opt-in; non-local DB host accepted.

## RED evidence
- `tests/security/test_dev_identity_provisioning.py` with `dev_identity.py` moved aside: `ModuleNotFoundError: No module named 'test_support.dev_identity'`. **Deviation disclosed**: the module was drafted before the test file; RED was proven afterwards by removal, the same technique WU-02.2/02.3 used.
- Script guard: without opt-in → `REFUSED: NQUIRY_DEV_IDENTITY_PROVISIONING must be set …`, exit 3.
- Real-stack lane (`PYTHON=.venv/bin/python scripts/run_real_stack_e2e.sh --project=desktop`): stack started, three identities provisioned, **real login succeeded and redirected to `/workspaces`**, then failed at `getByLabel('Workspace name')`. The F02 product surface does not exist yet. 1 failed.

## GREEN evidence
- `test_dev_identity_provisioning.py`: 9 passed.
- `test_habb_grant_constraints.py`: 6 passed (was 5 passed / 1 failed on the demo-seeded DB).
- Real-stack F02 spec: RED by design until WU-02.11.

## Tests
Direct: 9 provisioning tests. Relation: provisioning → real `application.auth_handler.login` (test 2). Integration/sibling: F01 governance tables untouched (test 1 asserts zero rows).

## Architecture gates
Run at Field end (L8) with every other change. `scripts/` is not scanned by the test-only import gate by design; the script is the only importer of `test_support.dev_identity`.

## Runtime / Browser proof
L6: real uvicorn + real Postgres + real `next dev`, all started by the runner. L7: the RED run above (real Chromium, no mocking).

## Accessibility / responsive / visual
The spec uses role/label locators (`getByLabel`, `getByRole`). The lane has a `mobile` (Pixel 7) project besides `desktop`; both must pass at WU-02.11.

## Recursive DeepSweep
- The provisioned identity is a first-class `users` row. `CMD_ADD_MEMBER` (F01) already accepts any existing user id, so the identity enters governance only through that governed Command.
- Fixing the unscoped COUNT removes a known-red test that could hide a genuine regression with the same signature (Comprehension proof E11).

## Inverse DeepSweep
"B can log in" ← local credential ← dev provisioning (disclosed, identity-only) ← operator HD-3. No authority link starts here.

## First Broken Relation
For the F02 flow: the first failing relation in the browser is the missing F02 product surface. Backend root causes are addressed in WU-02.6+ (effect gate), not at the label.

## Authority boundaries / Human decisions used
HD-3. No authority creation. HD-7/HD-8 (participation) obtained this session and used by the spec (see WU-02.8).

## Limitations
- The runner reuses already-running :8000/:3000 processes (if any) instead of restarting them. A container API built from an older tree would be reused. The final proof restarts from the current tree.
- Identities accumulate in the local DB (unique per run). No cleanup (append-only history).

## Mock / Fixture / External ceiling
None in the lane. Identity provisioning is a disclosed DEV-ONLY identity source (not authority).

## Git state
Uncommitted.

## Result
PASS (lane + RED established).
