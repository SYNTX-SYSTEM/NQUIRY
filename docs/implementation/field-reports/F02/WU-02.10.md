# WORK UNIT REPORT
FIELD: F02 — CHALLENGE · SESSION · PARTICIPATION
WORK_UNIT: WU-02.10 — F02 frontend surfaces + restrained F11 foundation

## Mission
Make the governed F02 slice visible and clickable for a stakeholder: where the human is, what they may do, why something is unavailable, which state is canonical, which authority enables an action, and what was denied, rejected, stale or blocked. The frontend computes no authority.

## Relation materialized
`server capability projection → UI affordance` and `UI intent → semantic Command (expectedVersion + Idempotency-Key) → server re-read → rendered canonical state`, for Workspace, Challenge and Session.

## Initial state
F01 pages (Workspace list, orientation + add member), unstyled. PKG-28/29 Session view at `/workspaces/{w}/sessions/{s}` showing the fixture-seeded Session. No Challenge or Session creation, no transitions, no participation UI. The root route redirected logged-in users to the seeded NON_PROOF demo Session.

## Files changed
- NEW `apps/web/app/globals.css`: tokens (light/dark), layout primitives (shell, panel, grid-2 → 1 column ≤ 860px), status semantics (outcome kinds, `unavailable`, `tag.authority/human/ai/fixture`), phase stepper, focus-visible ring, reduced-motion. No green = truth, no red = false. Color never carries meaning alone.
- MOD `apps/web/app/layout.tsx` (stylesheet, description)
- NEW `apps/web/components/f02/{AppShell,Outcome,Unavailable,LoadFailure}.tsx`
- NEW `apps/web/lib/api/inquiryClient.ts`: one envelope parser (all outcome kinds distinct + `network_failure`; fail-closed on unknown bodies), `newIntentKey()`, typed payloads
- REWRITE `apps/web/app/workspaces/[workspaceId]/page.tsx`: F01 orientation contract kept (testids unchanged); F02 members, Challenges, Challenge creation gated by the server `createChallenge` capability; NON_PROOF label from provenance (`governedFounding=false`)
- MOD `apps/web/app/workspaces/page.tsx`: restyle; label "Workspace name"; founding explanation
- NEW `apps/web/app/workspaces/[workspaceId]/challenges/[challengeId]/page.tsx`
- NEW `apps/web/app/workspaces/[workspaceId]/sessions/[sessionId]/page.tsx`: inquiry position (canonical state + version), next lawful step (server `relevant` + `available` + `reason`), protected Burst panel, participants + admission, phase list (`aria-current="step"`, no clickable tabs), authority provenance (holder, grantor, scope), "current state established by" (command, actor, authority source type + scope, commit), governance grant per Session
- MOVED PKG-28/29 page → `apps/web/app/workspaces/[workspaceId]/sessions/[sessionId]/decision/page.tsx` (imports adjusted, header note). Mocked specs retargeted (`session-view.spec.ts`, `decision.spec.ts`: `PAGE_PATH` + attacker path)
- MOD `apps/web/app/page.tsx`: root → `/workspaces` always (the seeded demo is no longer the landing page)
- MOD `apps/web/components/LogoutButton.tsx` (class only)
- MOD `apps/web/tests/e2e/workspaces.spec.ts`: `beforeEach` fulfils the new `/overview` read (component-contract isolation, MOCKED lane)
- NEW `apps/web/tests/lib/inquiryClient.test.ts` (12)
- NEW `apps/web/tests/real-stack/f02-accessibility.real.spec.ts`; MOD `f02-inquiry-context.real.spec.ts` (visual-proof screenshots)
- Backend support made in this WU: `inquiry_queries` action `relevant` flag (topology relevance computed server-side, so the UI does not re-derive it); `governedFounding` (FOUNDING audit exists) via `inquiry_directory.has_governed_founding`
- NEW `docs/implementation/field-reports/F02/visual/*.png` (6 curated real-stack screenshots)

## Test-first intent
- TRUE: the real-stack F02 spec (written in WU-02.5, RED since then) passes unchanged in intent; every Command outcome is visible as `data-outcome=<kind>`; unavailable actions show the server's reason and render no control.
- IMPOSSIBLE: a control rendered from a client role check; a disabled-but-present authority button; a client-assumed state after a Command; the fixture Session presented unlabelled.

## RED → GREEN
- RED: `f02-inquiry-context.real.spec.ts` failed in WU-02.5 at the first F02 surface (`getByLabel('Workspace name')`) and stayed RED through WU-02.6–02.9, because each backend layer alone could not satisfy it.
- GREEN (real stack, no mocking): desktop **1 passed**, mobile (Pixel 7) **1 passed**. Accessibility spec: desktop + mobile **2 passed**.
- Unit: `inquiryClient.test.ts` 12 passed; vitest total **79 + 12**. Mocked component-contract lane: **37/37 passed** after the retargeting. `tsc --noEmit` clean, `eslint` clean.

## Accessibility proof
- axe-core 4.13 (already installed through eslint-plugin-jsx-a11y; no new dependency) injected into the real pages: **0 serious/critical WCAG 2 A/AA violations** on Workspace list, Workspace, Challenge, Session DRAFT and Session SETUP, desktop and mobile.
- **Negative control** (one-off, outside the repo): the same injection on a deliberately broken page reported `button-name:critical, image-alt:critical, label:critical, document-title:serious`. The check is not vacuous.
- Keyboard-only: the controller reaches "Begin setup" with Tab, sees a visible focus outline, activates it with Enter, and the canonical state becomes SETUP.
- Semantics: skip link, `nav[aria-label=Breadcrumb]`, one `h1` per page, labelled inputs, `role=alert` for problem outcomes / `role=status` for commits, `aria-current="step"` in the phase list.

## Responsive proof
Both real-stack specs run at 1280×860 and Pixel 7. `scrollWidth − clientWidth ≤ 1` asserted on every F02 page. The mobile phase list shows passed + current + the next steps only (the full list stays in the DOM order for desktop).

## Visual proof
`docs/implementation/field-reports/F02/visual/`: owner Workspace (Facilitator-only reason), DRAFT without Session control (HD-1 reason), stale outcome, blocked without participant (mobile), QUESTION_GENERATION for the controller (burst ACTIVE, HUMAN_ONLY, authority provenance, establishing commit), and the same state for the Owner (no controls). Reviewed by eye. The floating "N" badge is the Next.js dev-mode indicator (not in production builds).

## Recursive DeepSweep / reconstruction chain
- **Changed relation**: server projection ↔ human perception.
- **Parent**: 20 §13 (frontend-first ≠ frontend-defined semantics). 19 §22 "Navigation reflects state; it does not create state".
- **Siblings**: F01 orientation contract preserved. The PKG-28/29 Decision surface moved to `/decision` (F07 will re-home it onto a lawfully reached Decision). The root no longer lands on the NON_PROOF Session.
- **Governance / authority**: every affordance comes from `inquiry_queries`. Owner, Facilitator and outsider see different surfaces for the same canonical state.
- **Persistence / API**: unchanged except the two read-model flags.
- **Tests**: unit + mocked + real-stack as above.
- **Reconstructed Field**: the stakeholder path is fully clickable.

## Inverse DeepSweep (visible QUESTION_GENERATION)
UI `session-state` ← `position.session.state` (server) ← `SessionRepository.get` (canonical row) ← `CMD_OPEN_QUESTION_GENERATION` commit (`establishedBy.commitId`) ← BND-014 BINDING `SESSION:<id>` (`establishedBy.authorityScopeRef`) ← binding shown in `session-authority-provenance` with its grantor (the governance root) ← membership (Members list) ← FOUNDING (no NON_PROOF label) ← real login. Every link is visible on the page and asserted in the real-stack spec.

## First Broken Relation
Two found and fixed at their homes:
1. The mocked Workspace spec would have been redirected to /login by the real API's 401 for the unmocked `/overview`. Isolation was fixed in the mocked spec. The page's session-expiry redirect is correct behavior.
2. Topology relevance would have been re-derived in the client. It was moved to the server (`relevant`).

## Authority boundaries / Human decisions used
HD-1, HD-3, HD-6, HD-7, HD-8 as materialized server-side. The UI adds none.

## Process incident (disclosed)
While moving the legacy page, `git mv` **staged** a rename in the index, which violates the no-stage law. It was detected immediately (`git diff --cached`) and undone with `git restore --staged` on exactly those two paths. The index is empty again. Nothing was committed.

## Limitations
- Member addition still takes a raw user id (F01 contract; no user-directory query exists, and adding one is a privacy/product question).
- Reason texts are English, server-generated.
- The full semiotic system (typography scale, motion, human/AI field transformation) is F11.

## Mock / Fixture / External ceiling
Mocked lane = component contract only (relabelled). Real-stack lane = runtime proof. Fixture Workspaces are labelled NON_PROOF from provenance.

## Git state
Uncommitted, index empty.

## Result
PASS.
