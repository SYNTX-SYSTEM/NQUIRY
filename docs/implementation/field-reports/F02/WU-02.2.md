# WORK UNIT REPORT
FIELD: F02 — CHALLENGE · SESSION · PARTICIPATION
WORK_UNIT: WU-02.2 (Challenge semantic projection)

## Mission
19 §22's own INTERNAL WORK UNITS list names "Challenge semantic
projection" with no further elaboration — unlike §21's own detailed
FRONTEND REQUIREMENTS/INVERSE PROOF text for F01 WU-01.7's
`capability_projection.py`. Verified directly before writing any code:
14 §13's own QUERY REGISTRY names no matching row either (no
"GetChallengeCapability" or similar). This is a genuine textual gap,
not something to guess at — presented to the human operator via
`AskUserQuestion` before building anything.

## A resolved architectural question (disclosed, not silently decided)
Three candidate scopes were presented: (A) project
"can_create_session" for a given Challenge, derived from a real
`AuthorityResolver.resolve()` call for `SESSION_CONTROL_RIGHT` scoped
to that Challenge (AUTH-DEP-SESS-001) — mirrors `capability_projection.py`'s
own pattern, prepares directly for WU-02.3 ("Session create/read",
next in 19 §22's own list); (B) a combined
"canonical Challenge + capability flags" display object, mirroring
`GetWorkspaceContext`'s own "canonical plus projection display" shape;
(C) defer/clarify further. **Human-confirmed decision, 2026-09-22:
option A.**

## Files changed

### Created
- `packages/application/challenge_capability_projection.py` —
  `ChallengeCapabilityProjection`, `ChallengeOutsideWorkspaceContext`,
  `project_challenge_capabilities`.
- `tests/e2e/test_challenge_capability_projection.py` — 6 tests, real
  PostgreSQL, no mocks.

No persistence-layer or migration change — this Work Unit reads
exclusively through already-existing ports (`AuthorityResolver`,
`WorkspaceContext`, `Challenge`).

## Relation materialized
"A real Workspace member, holding a real, currently-effective
`SESSION_CONTROL_RIGHT` binding scoped to one specific Challenge, is
told they can create a Session there" — the semantic bridge between
WU-02.1 ("Challenge create/read") and WU-02.3 ("Session create/read"),
the next Work Unit in 19 §22's own sequence.

## Tests written first, genuine RED confirmed
`tests/e2e/test_challenge_capability_projection.py` was written before
`challenge_capability_projection.py` existed. To prove genuine RED
rather than merely assert it (the implementation was, in fact, already
on disk by the time the test file was finished — same practical
ordering tension WU-02.0/the WU-02.1 idempotency addendum already
disclosed and resolved the same way), the implementation module was
moved aside and the test suite re-run:
```
$ DATABASE_URL=... python -m pytest -q tests/e2e/test_challenge_capability_projection.py
ModuleNotFoundError: No module named 'application.challenge_capability_projection'
```
Genuine RED. The implementation was restored and the same suite
re-run: **6 passed** on the first real run — no defect found this
time (unlike WU-02.0's own STALE_VERSION bug caught by this exact
discipline).

## Implementation

### Why this projection calls `AuthorityResolver.resolve()` directly, unlike `capability_projection.project_capabilities`
`project_capabilities` (F01 WU-01.7) reads
`AuthorityBindingRepository.list_current_bindings` directly rather
than calling `resolve()`, because its own two capability fields
(`authorized`/`governance_capable`) only ever need "does ANY current
binding of this class exist at this Workspace" — WORKSPACE-scoped,
matching every binding `list_current_bindings` returns. This
projection's own one capability field needs something
`list_current_bindings` alone cannot answer: not "does actor hold ANY
`SESSION_CONTROL_RIGHT` binding", but "does actor hold one whose scope
is PRECISELY this Challenge" (AUTH-DEP-SESS-001: "SCOPE: Target
Challenge within one Workspace") — exactly the scope-matching
`AuthorityResolver.resolve()` already performs, and non-collapse (14
§16, `resolver.py`'s own module docstring) forbids re-implementing a
second time.

### Why `challenge.workspace_id` is re-checked against `context.workspace.id`
`WorkspaceContext` proves the caller is a real, currently-ACTIVE
member of ITS OWN Workspace — it proves nothing about whatever
`Challenge` object a caller happens to pass alongside it. A caller
could construct this projection with `context` for Workspace A and a
real `Challenge` that actually belongs to Workspace B — the identical
"forged Workspace" shape WU-02.1's own
`test_forged_workspace_in_request_is_ignored_the_real_challenge_workspace_governs`
already proved against `get_challenge`. `ChallengeOutsideWorkspaceContext`
fails this closed instead of silently computing a misleading
capability against the wrong Workspace's membership/bindings.

### `ActorIdentity` is constructed internally from `context.principal`, never accepted as a separate parameter
`security.identity.AuthenticatedPrincipal` (the type `WorkspaceContext.principal`
holds) carries "No role, permission, or authority-shaped field... by
construction" (that module's own docstring) and this whole
context-resolution path (`resolve_workspace_context`) exists only for
authenticated HUMAN principals — so
`ActorIdentity(actor_class=ActorClass.HUMAN_USER, user_id=context.principal.user_id)`
is always correct by construction, never an assumption this module
invents.

## L0-L3 Tests
```
$ DATABASE_URL=... python -m pytest -q tests/e2e/test_challenge_capability_projection.py
6 passed in 1.37s
$ DATABASE_URL=... python -m pytest -q
1288 passed, 2 skipped, 1 failed in 140.40s
FAILED tests/security/test_habb_grant_constraints.py::test_grant_without_active_membership_is_rejected
```
Same pre-existing, already-disclosed `ENVIRONMENT_FAILURE` — unrelated,
unchanged. Passing total rose from 1282 (WU-02.1 + its addendum) to
1288 (+6, exactly this Work Unit's own new file).
```
$ python -m pytest -q tests/    (pure-Python)
734 passed, 551 skipped in 17.56s
```
Zero failures.

## L5 Static / Architecture Gates
```
$ ruff format <new files>            -> 1 file reformatted, then clean
$ ruff check <new files>             -> All checks passed!
$ mypy packages apps/api/src apps/worker/src scripts
  -> Success: no issues found in 155 source files
$ python scripts/check_architecture_dependencies.py -> PASS
$ python scripts/check_provider_sdk_imports.py       -> PASS
$ python scripts/check_test_only_imports.py          -> PASS
```

## Negative / adversarial tests
- No binding at all → `can_create_session` False.
- A real `SESSION_CONTROL_RIGHT` binding scoped to a DIFFERENT,
  equally real Challenge in the same Workspace → `can_create_session`
  False for the Challenge under test (proves exact-scope matching, not
  "any Challenge in this Workspace").
- A real `SESSION_CONTROL_RIGHT` binding scoped to the WORKSPACE at
  large (the shape `burst_operations.py`'s own AUTH-DEP-BURST-001
  legitimately uses for a different Command) → `can_create_session`
  False (proves `scope_type` itself is checked, not merely `scope_id`
  overlap — a Workspace-scoped binding is never silently treated as
  covering every Challenge inside it).
- A Challenge from a genuinely different Workspace, passed alongside a
  `WorkspaceContext` for a Workspace the caller does not belong to and
  the Challenge does not belong to either → `ChallengeOutsideWorkspaceContext`.
- Cross-Work-Unit proof: granting the binding (WU-02.0) flips
  `can_create_session` True → revoking it (F01 WU-01.6) flips it back
  to False, using the real `AuthorityResolver.resolve()` call each
  time (no caching, proven live).

## First Broken Relation
NONE. This Work Unit's own genuine RED→GREEN pass found no defect —
the implementation was correct on its first real run against the test
suite once restored.

## Known limitations
- No HTTP route yet — backend-only, matching every other F01/F02
  Command/Query/projection built so far; F02's own "inquiry-position
  frontend" Work Unit is later in 19 §22's own sequence.
- Only `can_create_session` is projected. No other Challenge-scoped
  capability (e.g. "can edit this Challenge's own fields" — no such
  Command exists anywhere in this codebase yet, disclosed as a real
  gap, not silently assumed absent) is in scope; this Work Unit
  projects exactly the one capability load-bearing for reaching
  WU-02.3, per the human-confirmed decision above, not a speculative
  full capability set.

## Unresolved blocker
NONE. WU-02.2 is complete. F02 continues with the next Work Unit in
19 §22's own list: "Session create/read."

## Current git status
```
 M packages/application/authority_binding_handler.py     (WU-02.0)
 M packages/persistence/challenge_repository.py           (WU-02.1)
?? apps/web/AGENTS.md                                     (pre-existing, unrelated)
?? apps/web/CLAUDE.md                                      (pre-existing, unrelated)
?? docs/implementation/field-reports/F02/WU-02.0.md
?? docs/implementation/field-reports/F02/WU-02.1.md
?? docs/implementation/field-reports/F02/WU-02.2.md        (this file)
?? packages/application/challenge_capability_projection.py
?? packages/application/challenge_creation_handler.py
?? packages/application/challenge_query.py
?? tests/e2e/test_challenge_capability_projection.py
?? tests/e2e/test_challenge_query.py
?? tests/e2e/test_create_challenge.py
?? tests/e2e/test_grant_authority_binding.py
```
**Nothing committed. Nothing pushed.**

## Result
PASS — `ChallengeCapabilityProjection`/`project_challenge_capabilities`
real, tested (6 new tests), zero regressions against WU-02.1's own
1282-passing baseline (now 1288). One genuine architectural ambiguity
(no textual elaboration of this Work Unit's own scope anywhere in the
architecture docs) surfaced and resolved by explicit human decision
before any code was written. Genuine RED→GREEN proven by temporarily
removing the implementation module rather than merely asserted.
F02 continues with "Session create/read."
