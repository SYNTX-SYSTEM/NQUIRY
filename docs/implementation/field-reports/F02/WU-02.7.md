# WORK UNIT REPORT
FIELD: F02 — CHALLENGE · SESSION · PARTICIPATION
WORK_UNIT: WU-02.7 — HD-1 Session-scoped control; semantic Session transitions; Burst preparation; TRN-SESS-004 bundle

## Mission
Make QUESTION_GENERATION lawfully reachable through one semantic Command per architecture-defined transition. Session authority must resolve at exactly `SESSION:<id>` (HD-1).

## Parent / Current Field
03 §13–§22 (TRN-SESS-002/003/004, TRN-BURST-001/002), 04 §23–§25, §35–§36 (AUTH-DEP-SESS-002/003/004, BURST-001/002), 12 §10, NQ-DEC-016/017, HD-1 (16 §41 REC-002), HD-8 (REC-007). F02.

## Relation materialized
`authorized Session controller (SESSION_CONTROL_RIGHT @ SESSION:<id>) → CMD_BEGIN_SETUP / CMD_BEGIN_CHALLENGE_CAPTURE / CMD_PREPARE_BURST / CMD_OPEN_QUESTION_GENERATION → BND-001/002/003/005/007 → BND-014 (BINDING, exact scope) → canonical Session/Burst write + typed audit`.

## Initial state
- `SessionRepository` read-only plus `create` (WU-02.3). No transition write existed.
- `burst_operations` (PKG-07) resolved Burst control at WORKSPACE scope. CreateSession resolved it at CHALLENGE scope. 04 says "Specific Session". Three scopes for one right (Comprehension proof E5).
- `grant_human_authority_binding` (WU-02.0) accepted any scope id, including nonexistent or foreign ones.

## Target state
Explicit, testable lineage:
`CHALLENGE-scoped grant → CreateSession (AUTH-DEP-SESS-001, scope "Target Challenge") → governance root grants SESSION-scoped control explicitly → transitions`.
No inheritance, no automatic side-effect grant.

## Reconstructed architecture fact (disclosed, derived, not invented)
03 TRN-SESS-004 is **coupled to QuestionBurst start**: "Session → QUESTION_GENERATION. QuestionBurst → ACTIVE. The Session and Burst state changes form one logical transition bundle… A partial pair is invalid architecture state." Reaching QUESTION_GENERATION lawfully therefore requires, inside F02:
- a PREPARED Burst (TRN-BURST-001, AUTH-DEP-BURST-001 "Specific Session");
- the coupled Burst start.

F02's own target includes QUESTION_GENERATION, so this is F02 scope. Question capture, pause/resume, completion and the frozen set stay F03.

## Case-3 boundaries hit and resolved by the operator this Field
- **GAP-09-007** (who establishes a SessionParticipation): HD-7 "Controller admits" (16 §41 REC-006). See WU-02.8.
- **TRN-SESS-004 "required participant/configuration context"**: HD-8 "≥1 participant" (REC-007).

## Case-1/Case-2 decisions (recorded)
- AUTH-DEP-SESS-004 / BURST-002 deny a "Non-Facilitator without explicit source-equivalent binding". Under HD-1 the Session-scoped `SESSION_CONTROL_RIGHT` binding **is** that explicit binding. No Workspace role is consulted (04 §2.3 "Role alone does not grant authority"). Case 1.
- TRN-SESS-003 "method-required setup structurally present": the pinned `applied_method_key/version` exist (12: one pinned method). Case 1.
- TRN-BURST-001 "pre-generation state compatible with later start": DRAFT/SETUP/CHALLENGE_CAPTURE. Case 2.
- One Burst per Session: handler precondition `BURST_ALREADY_EXISTS`, plus DB partial unique index (see WU-02.8). Case 2, follows 03 topology and `get_by_session`'s one-row read.
- Expected-version mismatch is **STALE**, raised before boundaries (a stale view is not an authority denial). BND-014 re-checks the same version at commit. Case 1 (19 §29 outcome distinctions).
- Unmet preconditions are **BLOCKED** (`SessionPreconditionUnmet`), recorded as `DENIED` with `failure_code=PRECONDITION_UNMET` on the command attempt (CommandOutcome is a closed 4-value vocabulary). Case 2.

## Files changed
- NEW `packages/application/session_control_handler.py`: `begin_setup`, `begin_challenge_capture`, `prepare_burst`, `open_question_generation`, `admit_participant`, `CommandIdentity`, typed exceptions
- MOD `packages/persistence/session_repository.py`: `transition()` (state + version guarded), `SessionTransitionConflict`
- MOD `packages/persistence/burst_repository.py`: `SqlAlchemyBurstVersionReader`, `burst_target_ref`
- MOD `packages/application/authority_binding_handler.py` (WU-02.0 WIP): `GrantScopeInvalid`, `_require_scope_in_workspace` (scope must be a record of the same Workspace; types WORKSPACE/CHALLENGE/SESSION/DECISION)
- MOD `packages/application/burst_operations.py`: SESSION scope (HD-1), `session_id` parameter; PKG-07 WORKSPACE-scope reading marked superseded
- NEW `tests/e2e/test_session_control.py` (14); MOD `tests/e2e/f02_support.py`; MOD `tests/e2e/test_grant_authority_binding.py` (+1 test; fixture now uses real Challenges); MOD `tests/transitions/test_burst_operations_readiness.py` (SESSION scope, +1 test)

## Test-first intent / RED / GREEN
- TRUE: lawful walk to QUESTION_GENERATION with Burst ACTIVE in the same commit; audit BINDING at `SESSION:<id>` naming the real binding; both bundle targets on one audit row.
- IMPOSSIBLE: CHALLENGE-scope, WORKSPACE-scope or other-SESSION-scope control moving the Session; Owner without a Session binding; stale commit; cross-Workspace actor (three variants: foreign claimed Workspace, real Workspace as non-member, unknown Workspace); DRAFT → CHALLENGE_CAPTURE (BND-007); opening without a Burst or without a participant; second Burst; partial bundle (Burst advanced underneath → `CommitFailedPrecommit`, Session unchanged, Burst unchanged); grant for a missing or foreign scope.
- RED: `test_session_control.py` with the handler moved aside → `ModuleNotFoundError: application.session_control_handler`. **Deviation disclosed**: the handler was drafted before the test file. RED was proven by removal (same technique as WU-02.2/02.3/02.5). Grant-scope test: 9 existing WU-02.0 tests went RED against the new rule (they granted bindings for random nonexistent Challenges). The fixture was corrected to real Challenges and the new rule kept.
- GREEN: session control 14/14, grant 12/12, transitions 246/246.

## Tests
Direct: 14 session-control tests. Producers: grant, CreateSession, CreateChallenge (via `f02_support.inquiry_context`, all real Commands). Consumers: `GetSession` view (reads the new states). BND-007 (`terminal_boundary_id is BND_007` asserted).

## Field integration / sibling regression
Full backend, live DB: **1338 passed, 2 skipped, 0 failed** (after the RLS-set update in WU-02.8). ruff, mypy (162 files) and the architecture gate PASS.

## Runtime / browser proof
Not yet visible. HTTP + UI in WU-02.9/02.10, real-stack proof in WU-02.11.

## Recursive DeepSweep / reconstruction chain
- **Changed relation**: Session control authority → Session/Burst state.
- **Parent**: 04's per-operation scopes now hold literally. 12 §10's state table is reachable through real Commands up to QUESTION_GENERATION.
- **Affected siblings**: F03 inherits a PREPARED→ACTIVE Burst and SESSION-scoped control, so capture/complete must also use `SESSION:<id>`. `burst_operations` is reconciled now so F03 does not start from the stale WORKSPACE scope. F05 `select_question` uses QUESTION_SELECTION_RIGHT at SESSION scope, already consistent.
- **Governance**: the governance root must explicitly grant Session control per Session. This is visible friction, and deliberately so: governance, not convenience.
- **Authority**: BND-005 (precommit) and BND-014 (commit) resolve the identical `SESSION:<id>` binding.
- **Persistence**: guarded transition write + DB trigger (legal pairs, version advance) + partial unique open-Burst index.
- **API / frontend**: to be materialized. Stale/blocked/denied must render distinctly.
- **Tests**: +16 tests. Grant fixture corrected.
- **Reconstructed Field**: DRAFT → … → QUESTION_GENERATION is real at the application layer. F02 remains open until HTTP, UI and the real browser exist.

## Inverse DeepSweep (application layer)
Session QUESTION_GENERATION ← `sessions.state` ← CMD_OPEN_QUESTION_GENERATION commit ← BND-014 BINDING ← `human_authority_bindings` (SESSION scope, granted via CMD_GRANT_HUMAN_AUTHORITY_BINDING by the WORKSPACE_GOVERNANCE_RIGHT holder) ← ACTIVE membership (CMD_ADD_MEMBER) ← FOUNDING (CMD_CREATE_WORKSPACE) ← verified identity. Asserted row by row in `test_lawful_path…`.

## First Broken Relation
Repaired: "Session control authority ↔ its scope" (three divergent scopes). Also repaired: "binding scope ↔ scoped record" (grants for records that don't exist or belong elsewhere).

## Authority boundaries / Human decisions used
HD-1, HD-8 (and HD-7 via WU-02.8). No automatic authority creation anywhere.

## Limitations
- `SessionNotFound` for an unknown session id can let a member distinguish "no such session" from "denied". Accepted: requires Workspace membership plus a guessed UUID.
- Pause/resume/complete and capture: F03.

## Mock / Fixture / External ceiling
None. Test preconditions for the grant suite insert Challenge rows directly (that suite tests grants). Every F02 flow test uses real Commands.

## Git state
Uncommitted.

## Result
PASS (application layer). Visible layers pending.
