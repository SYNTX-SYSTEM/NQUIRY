# WORK UNIT REPORT
FIELD: F02 — CHALLENGE · SESSION · PARTICIPATION
WORK_UNIT: WU-02.6 — Effect gate typed authority sources (HD-6): BINDING / ROLE / FOUNDING

## Mission
Repair the First Broken Relation found by the architecture sweep (Comprehension proof E3/E4): canonical writes beside `CommitCoordinator` (CreateWorkspace, CreateChallenge), and audit provenance fabricated with `uuid.uuid4()`. Repair at the authoritative home: the effect gate itself.

## Parent / Current Field
16 §41 REC-004 (NQ-DEC-034, HD-6); NQ-DEC-011/014; 06 BND-014; 09 audit contract. F02.

## Relation materialized
`CANDIDATE → BND-014 (typed authority) → CommitCoordinator → canonical write + audit(typed, real provenance)` for every governed Command, including role-sourced (AUTH-DEP-CH-001) and founding (HARD-DEP-001 Option A) ones.

## Initial state
- `Bnd014Input` accepted only an `AuthorityClass` + scope. BND-014 could express BINDING authority only.
- `CommitCoordinator._commit_inner`: `authority_source_ref = binding_id if present else uuid.uuid4()` (`coordinator.py:438-442`).
- CreateWorkspace and CreateChallenge each hand-rolled `connection.begin_nested()` + CommitUnit + audit + outbox. CreateChallenge's audit ref = `uuid.uuid4()`. CreateWorkspace's audit ref = the binding it created itself (circular).

## Target state
One effect gate. Typed sources. Every audit row written from now on names a real record of the named type, at the exact scope.

## Files changed
- NEW `packages/boundaries/authority_source.py`: `AuthoritySourceType`, `BindingAuthority`, `RoleAuthority`, `FoundingAuthority`, `AuthoritySourceProof`
- MOD `packages/boundaries/types.py`: `BoundaryProof.authority_source` (optional, trailing)
- MOD `packages/boundaries/bnd_014_commit.py`: typed `authority` (legacy triple normalized to BINDING), `command_ref`, ROLE evaluation via `MembershipRepository` (fresh current membership + current role), FOUNDING evaluation (human actor only), version 1.1, no ALLOW without a proof
- MOD `packages/commit/coordinator.py`: `commit(authority=…)`, legacy params optional; refuses an ALLOW without `authority_source` (`EFFECT_GATE_NO_AUTHORITY_SOURCE`); uuid4 fallback removed; FOUNDING records the attempt inside the SAVEPOINT; `MutationOutcome.governance_refs/event_type/result_ref`; audit/commit `target_refs` include `created_refs`
- MOD `packages/command/envelope.py`: `created_refs` (created records; not version-checked; disjoint from targets)
- NEW migration `a7f2c91d4e10_typed_authority_provenance.py`: `audit_events.authority_source_type` (CHECK BINDING/ROLE/FOUNDING), `authority_scope_ref`, "set together" CHECK. NULL = pre-F02 untyped historical row (audit rows are immutable)
- MOD `packages/persistence/tables.py`, `packages/audit/models.py`, `packages/persistence/audit_repository.py`
- MOD `packages/application/workspace_creation_handler.py`: FOUNDING via coordinator (`_FoundWorkspaceMutation`), docstring updated
- MOD `packages/application/challenge_creation_handler.py` (F02 WIP): ROLE via coordinator (`_CreateChallengeMutation`), fabricated ref removed, docstring updated
- NEW `packages/application/composition.py` (`GovernedPorts`: one construction of real ports for dispatch and tests)
- NEW tests `tests/boundaries/test_bnd_014_typed_authority.py` (9), `tests/e2e/test_typed_authority_provenance.py` (4), `tests/e2e/f02_support.py`
- MOD `tests/e2e/test_create_workspace.py`: 3 assertions (see "semantic changes")

## Test-first intent
- TRUE: ALLOW ⇒ `authority_source` with a real ref (binding id / role_assignments id / commands id) and exact scope; audit rows carry type + scope.
- IMPOSSIBLE: ALLOW without a source; ROLE ALLOW for non-member, revoked member, wrong role, or with no role reader configured; FOUNDING for a non-human actor; BINDING ALLOW at another Session scope; any `begin_nested` writer in `packages/application` except the documented recovery exception.
- FALSIFIER: any new audit row whose ref does not resolve.

## RED evidence
- `test_bnd_014_typed_authority.py` before implementation: 9 failed (`ImportError`/`unexpected keyword argument 'authority'`).
- `test_typed_authority_provenance.py` before the handler refactor: founding ref ≠ command; challenge ref unresolvable; static check listed `challenge_creation_handler.py`, `workspace_creation_handler.py`.

## GREEN evidence
- Boundaries: 332 passed. Provenance + CreateChallenge + CreateWorkspace: 30 passed.
- Full backend (live PostgreSQL, demo-seeded DB): **1322 passed, 2 skipped, 0 failed** (F00 baseline: 1175/2/1).
- Gates: ruff check/format PASS, mypy PASS (160 files), architecture dependency PASS, provider-SDK PASS, test-only-import PASS, migrations PASS (21 revisions, head `a7f2c91d4e10`).

## Semantic changes to existing tests (disclosed, authorized by HD-6)
1. `test_real_human_founder_creates_workspace…`: audit ref was asserted to equal the founding binding. Now FOUNDING + ref = the founding command. The old assertion encoded circular provenance.
2. `test_failed_founding_…INJECTED_AUDIT_FAILURE`: exception type is now `CommitFailedPrecommit` (message keeps `INJECTED_AUDIT_FAILURE`). This matches every other gated Command. Atomic-rollback assertions are unchanged and pass.
3. `test_duplicate_attempt_id…`: same classification (`CommitFailedPrecommit`, message keeps `AttemptAlreadyRecorded`). No second Workspace, unchanged.

## Direct / producer / consumer tests
Direct: BND-014 typed tests. Producers: CreateWorkspace, CreateChallenge, AddMember (BINDING). Consumers: audit repository round-trip (`_event_from_row`), commit units (`governance_refs`), outbox (`WORKSPACE_CREATED`, `CHALLENGE_CREATED` preserved).

## Field integration / sibling regression
F01 suites (create workspace, add member, revoke, grant, capability projection, accessible workspaces) all green. PKG-era commit/idempotency/outbox/projection suites green.

## Runtime / Browser proof
L6: migration applied to the live DB. L7: deferred to WU-02.11 (no visible change yet).

## Recursive DeepSweep / reconstruction chain (Documentation Hard Law)
- **Changed relation**: authority → effect gate → audit provenance.
- **Parent**: NQ-DEC-014 now actually holds for role-sourced and founding Commands. 20 §7's typed-source table is now true in code.
- **Affected siblings**: F01 CreateWorkspace (moved behind the gate). F09 recovery_handler still has its own SAVEPOINT (OPERATIONAL RecoveryRecord, 10 §63: no CommitUnit by design), named as the single documented exception in the static test.
- **Governance**: founding binding recorded as `CommitUnit.governance_refs`, not as authority source.
- **Authority**: BND-014 ROLE check is fresh at commit. A Facilitator whose role or membership is revoked between prepare and commit is now denied at commit; previously CreateChallenge re-checked only the Workspace version.
- **Persistence**: two nullable audit columns + CHECKs. Pre-F02 rows stay NULL (untyped), because audit is immutable. The fabricated historical refs remain in history and are identifiable as `authority_source_type IS NULL`.
- **API / frontend**: no change yet. Provenance becomes displayable (WU-02.9/02.10).
- **Tests**: +13 tests, 3 semantic updates.
- **Reconstructed Field**: all governed writes in F02 now pass through one effect gate. F03 Burst/capture Commands inherit typed sources without new mechanisms.

## Inverse DeepSweep
`audit_events(CMD_CREATE_CHALLENGE).authority_source_ref` ← ROLE ← `role_assignments.id` (Facilitator, current at commit) ← `workspace_memberships` ACTIVE ← CMD_ADD_MEMBER by the governance root ← FOUNDING (`commands.id` of CMD_CREATE_WORKSPACE) ← verified identity. Every link is a real row (asserted in tests).

## First Broken Relation
Repaired: "effect gate ↔ non-binding authority". It was the root of both the parallel writers (E3) and the fabricated provenance (E4).

## Authority boundaries / Human decisions used
HD-6. No new authority semantics. ROLE is used only where 04 names a role right (AUTH-DEP-CH-001). FOUNDING only where HARD-DEP-001 Option A applies.

## Limitations
- CreateWorkspace has no idempotency key (an `idempotency_records` row FKs a Workspace that does not exist yet). The F01 route `POST /workspaces` is therefore not idempotent. Disclosed, not new.
- `CommitCoordinator.commit` still accepts the legacy BINDING triple (normalized) to avoid churning every PKG-era caller.

## Mock / Fixture / External ceiling
None.

## Git state
Uncommitted.

## Result
PASS.
