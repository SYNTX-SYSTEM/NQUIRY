# WORK UNIT REPORT
FIELD: F02 — CHALLENGE · SESSION · PARTICIPATION
WORK_UNIT: WU-02.8 — SessionParticipation (HD-7 controller admission) + one open Burst per Session

## Mission
Materialize the SessionParticipation relation required by 09 §28 and by TRN-SESS-004's participant precondition (HD-8), without inventing join authority (09 §82).

## Case-3 boundary (raised and closed during F02)
09 §82: "Participation mutation authority beyond source join semantics must not be invented." GAP-09-007 UNDERDEFINED. The executor asked the operator, with options (controller admits / self-join / both / defer). **HD-7: Controller admits.** **HD-8: ≥1 participant before Question Generation opens.** Reconciled into 16 §41 REC-006/REC-007 and 20 §15 in the same Work Unit (Documentation Hard Law).

## Relation materialized
`Session controller (SESSION_CONTROL_RIGHT @ SESSION:<id>) → CMD_ADMIT_SESSION_PARTICIPANT → session_participations(user ↔ session)`, with the participant required to be an ACTIVE member of the Session's Workspace.

## Initial state
No table, repository, command or query for participation (only 09 §28 contract and 12 §9 row).

## Files changed
- NEW migration `b3d8e5f0a2c7_session_participation.py`:
  - `session_participations` (09 §28 fields + `workspace_id` + `admitted_by_user_id`);
  - composite FK `(session_id, workspace_id) → sessions(id, workspace_id)`: no cross-Workspace row possible;
  - `trg_session_participation_requires_membership` (ACTIVE membership, BEFORE INSERT);
  - partial UNIQUE current participation `(session_id, user_id) WHERE left_at IS NULL`;
  - RLS `workspace_isolation` (same fail-closed policy as the 29 PKG-26 tables);
  - partial UNIQUE `question_bursts(session_id) WHERE state <> 'COMPLETED'` (03 "no competing active Burst"; closes the concurrent double-prepare race).
- MOD `packages/persistence/tables.py`
- NEW `packages/persistence/session_participation_repository.py`
- `admit_participant` in `packages/application/session_control_handler.py` (WU-02.7 file)
- MOD `tests/security/test_workspace.py`: RLS expected set + `session_participations`

## Test-first intent
- TRUE: controller admits an ACTIVE member; participation is visible; the bundle then opens.
- IMPOSSIBLE: admission by anyone without Session control (including the Owner); a non-member participant (handler and DB trigger both refuse); duplicate current participation; RLS-unprotected Workspace-keyed table.
- RED: the tests for admission lived in `test_session_control.py`, RED with the handler removed (WU-02.7). The RLS test went RED on the new table (`test_rls_is_enabled_on_every_workspace_scoped_table`), which is the intended guard for exactly this change, and was updated.
- GREEN: included in the 14 session-control tests. RLS 10/10.

## Recursive DeepSweep / reconstruction chain
- **Changed relation**: User ↔ Session participation.
- **Parent**: 02 §12.4 "Participation does not imply operation authority". Participation is never consulted as authority by any F02 Command. It is only a precondition (HD-8), and in F03 the source-explicit capture right (04 §40).
- **Siblings**: F03 capture (AUTH-DEP-Q-001 "Valid SessionParticipation") now has its relation. Leave/removal semantics remain GAP-09-007 remainder (not decided, not built).
- **Governance / authority**: admission is BINDING-sourced (Session control), audited with `SESSION:<id>` scope.
- **Persistence**: see files. `migrations` head `b3d8e5f0a2c7`.
- **API / frontend**: participants list + admit control follow in WU-02.9/02.10.
- **Tests**: see above.
- **Reconstructed Field**: HD-8 precondition is satisfiable lawfully.

## Inverse DeepSweep
participation row ← CMD_ADMIT_SESSION_PARTICIPANT commit ← BND-014 BINDING `SESSION:<id>` ← granted binding ← governance root; participant ← ACTIVE membership (trigger-proven).

## Limitations
- No leave/remove command (not decided).
- `SessionNotFound` vs. denied distinction: see WU-02.7.

## Mock / Fixture / External ceiling
None.

## Git state
Uncommitted.

## Result
PASS (application + persistence).
