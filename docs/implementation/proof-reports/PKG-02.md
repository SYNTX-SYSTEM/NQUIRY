# PKG-02 Completion Report — Membership and governance persistence

Executed per `docs/architecture/NQUIRY_IMPLEMENTATION_MASTER.md` §"PKG-02:
Membership and governance persistence" (COPY-PASTE CODING AGENT PROMPT),
grounded in `14_IMPLEMENTATION_SEQUENCE.md` §7-§10/§39/§46/§48, Home-Files
02 §7 (WorkspaceMembership), 04 §6-§9 (HABB definition, Delegation
Model, Decision Right Classes) + AC-04-001, 05 §8-§20 (HABB Lifecycle
through Facilitator Scope Result) + AC-05-003 through AC-05-012, and
09 §23-§24/§50-§51 (WorkspaceMembership/RoleAssignment/HABB/
FacilitatorScopeBinding data contracts).

`HUMAN_AUTHORIZED_SCOPE: PKG-02 ONLY`. Predecessor PKG-01 verified
(commit `35ef80a`, `PACKAGE_PASS`, human gate for Phase 1 given). No
successor package started.

## COMPLETION_REPORT

- **PACKAGE_ID**: PKG-02
- **PACKAGE_TITLE**: Membership and governance persistence
- **BUILD_PHASE**: 1
- **VERDICT**: `PACKAGE_PASS`
- **UPSTREAM_FILES_READ**: `14_IMPLEMENTATION_SEQUENCE.md` (§7 Database Architecture, §8 DB Principals, §9 Migration Plan, §10 Repository Ports, §39 test map, §46/§48 package/file maps, plus §0-§53 already read for PKG-00/01); `02_DOMAIN_AND_RELATION_MODEL.md` §7 (WorkspaceMembership); `04_AUTHORITY_AND_DECISION_RIGHTS.md` §3 (Actor Identity Model, already read PKG-01), §6 (Human Authority Binding), §7-8 (Delegation Model, System-Derived Procedural Authority), §9 (Decision Right Classes), AC-04-001 (already read PKG-01), GAP-04-005 (Facilitator Scope Binding); `05_GOVERNANCE_INSIDE_SYSTEM.md` §8 (HABB Lifecycle), §9-10 (GOV-001 Bootstrap, GAP-05-001), §11-20 (GOV-002 through GOV-008, Effectiveness Predicate, Facilitator Scope Result), §59-68 (AC-05-003 through AC-05-012); `09_DATA_EVENT_API_CONTRACTS.md` §23-24 (WorkspaceMembership, RoleAssignment data contracts), §50-51 (HumanAuthorityBinding, FacilitatorScopeBinding data contracts); `16_DECISION_GAP_REGISTER.md` (confirmed GAP-04-005 status `RESOLVED` by 05, GAP-02-001 status `OPEN`)
- **14_REQUIREMENTS_MATERIALIZED**: `WorkspaceMembership` + `RoleAssignment` (09 §23-24, kept as two tables per 09 §23.1); `HumanAuthorityBinding` (04 §6, 09 §50) with its exact 7-class closed `authority_class` vocabulary (04 §9); read-only `MembershipRepository`/`AuthorityBindingRepository` (14 §10); migration `002_membership_governance`
- **PREDECESSORS_VERIFIED**: PKG-01, commit `35ef80a`, `PACKAGE_PASS`, human gate for Phase 1 given by user
- **FILES_CREATED**: `migrations/versions/01a37c093cd6_membership_governance.py`; `packages/governance/{membership,authority_binding}.py` (`WorkspaceRole`, `MembershipStatus`, `AuthorityClass`, `AuthorityBindingState`); `packages/persistence/{membership_repository,authority_binding_repository}.py`; `tests/authority/{conftest,test_membership_repository,test_authority_binding_repository}.py`; `tests/security/{conftest,test_habb_grant_constraints}.py`
- **FILES_MODIFIED**: `packages/governance/__init__.py`, `packages/persistence/__init__.py` (docstrings); `packages/persistence/tables.py` (three new `Table` objects); `pyproject.toml` (`UP042` added to ruff ignore, same 3.10-sandbox-vs-3.13-target rationale as `UP017`); `scripts/check_architecture_dependencies.py` (`INTERNAL_ALLOWED["persistence"]` extended with `governance`, cited); `tests/regression/test_architecture_dependency_checks.py` (positive + negative control tests for that extension, added in the same pass — not after a review, unlike PKG-01)
- **FILES_DELETED**: none
- **MIGRATIONS_CREATED**: `002_membership_governance` (`01a37c093cd6`) — creates `workspace_memberships`, `role_assignments`, `human_authority_bindings`, plus two PL/pgSQL triggers on the latter. **Deliberately does NOT create `facilitator_scope_bindings`** — see KNOWN_LIMITATIONS. Verified live against real PostgreSQL 17: upgrade → inspect schema → downgrade (drops cleanly, including triggers/functions) → re-upgrade → six live constraint/trigger proofs (below) → full test suite re-run against a completely fresh database volume with both migrations applied from scratch.
- **SCHEMA_CHANGES**: `workspace_memberships(id, workspace_id FK, user_id FK, status CHECK ACTIVE|REVOKED, created_at, revoked_at, record_version)` + partial-unique(workspace_id, user_id) WHERE ACTIVE; `role_assignments(id, workspace_id FK, membership_id FK, role CHECK 5 source-literal values, granted_by_user_id FK, granted_at, revoked_at, record_version)` + partial-unique(membership_id) WHERE not revoked; `human_authority_bindings(id, workspace_id FK, human_user_id FK, authority_class CHECK 7 closed values, scope_type, scope_id, authority_source, granted_by_user_id FK, granted_at, revoked_by_user_id FK, revoked_at, state CHECK ACTIVE|REVOKED, record_version)` + `trg_habb_check_grant_preconditions` (BEFORE INSERT: active membership required, `WORKSPACE_GOVERNANCE_RIGHT` scope must equal workspace) + `trg_habb_check_immutable` (BEFORE UPDATE: 09 §50.1 immutable fields, 05 §8.1 REVOKED terminal)
- **DB_PRIVILEGE_CHANGES**: none (PKG-25 scope)
- **PUBLIC_INTERFACES_CREATED**: `governance.membership.{WorkspaceRole, MembershipStatus}`; `governance.authority_binding.{AuthorityClass, AuthorityBindingState}`; `persistence.membership_repository.{MembershipRecord, RoleAssignmentRecord, MembershipRepository, SqlAlchemyMembershipRepository}`; `persistence.authority_binding_repository.{HumanAuthorityBindingRecord, AuthorityBindingRepository, SqlAlchemyAuthorityBindingRepository}`
- **COMMANDS_CREATED**: `NOT_APPLICABLE` (no Command/CommitUnit infrastructure exists yet; PKG-02 prompt: "AUTHORITY: Persist authority facts only, do not resolve them")
- **QUERIES_CREATED**: `NOT_APPLICABLE`
- **EVENTS_CREATED**: `NOT_APPLICABLE`
- **BOUNDARIES_CREATED_OR_CHANGED**: none executable (PKG-02 prompt: "BOUNDARIES: No boundary engine yet"). The two database triggers enforce GOV-005's preconditions (active membership, Workspace-scoped governance right) as data-integrity constraints (14 §7.3: "trigger/constraint enforcement where SQL cannot express a cross-table invariant directly") — not a Boundary evaluation.
- **AUTHORITY_PATH**: `NOT_APPLICABLE` for resolution (no `AuthorityResolver`, PKG-03 scope). Structural-only: `human_authority_bindings` can now durably represent an authority fact; nothing consumes it as a decision input yet.
- **EVIDENCE_PATH**: `NOT_APPLICABLE`
- **AI_PATH**: `NOT_APPLICABLE` (PKG-02 prompt: "AI: AI actor must not receive human rights" — structurally guaranteed: `human_user_id`/`granted_by_user_id`/`revoked_by_user_id` all FK to `users`, which holds only human identities; no AI/service identity table exists to reference, proven live in `tests/security/test_habb_grant_constraints.py::test_binding_cannot_target_a_nonexistent_identity`)
- **RECOVERY_PATH**: `NOT_APPLICABLE`
- **TESTS_CREATED**: `tests/authority/test_membership_repository.py` (T3, 5 tests, live PostgreSQL); `tests/authority/test_authority_binding_repository.py` (T3, 5 tests, live PostgreSQL); `tests/security/test_habb_grant_constraints.py` (T9, 6 tests, live PostgreSQL, DB-trigger/constraint proofs); `tests/regression/test_architecture_dependency_checks.py` additions (T12, 2 tests: positive + negative control for `persistence → governance`)
- **TESTS_MODIFIED**: none
- **TARGETED_TEST_RESULTS**: 18 new tests, all pass against live PostgreSQL 17, both against an already-migrated instance and against a freshly created one with both migrations applied from scratch in this session
- **NEGATIVE_TEST_RESULTS**: `test_get_current_membership_returns_none_when_absent`, `test_get_current_membership_excludes_revoked`, all six `test_habb_grant_constraints.py` cases — all pass, asserting the canonical DB-level rejection (`DBAPIError` with the specific `RAISE EXCEPTION` message), not merely "some error occurred"
- **ADVERSARIAL_TEST_RESULTS**: all 6 package-mandatory attacks proven (exceeds the ≥5 minimum):
  1. Revoked HABB → `list_current_bindings` excludes it; history retains it. PASS.
  2. Absent membership → proven from **both** directions: (a) DB rejects the grant outright (`trg_habb_check_grant_preconditions`, live), and (b) the repository's conservative non-collapse boundary is proven separately — a binding whose membership was later revoked is *still* returned as a raw fact by `list_current_bindings`, with an explicit test and module-docstring warning that this is not an effectiveness check. PASS.
  3. Wrong Workspace binding → cross-workspace read isolation (repository level) + `WORKSPACE_GOVERNANCE_RIGHT scope_id must equal workspace_id` (DB trigger level, live). PASS.
  4. AI actor binding / SYSTEM_SERVICE given human right → structurally impossible (no identity table to reference); live-proven rejection. PASS.
  5. Owner assumed superuser → a membership with `role=Owner` and no explicit HABB yields zero bindings. PASS.
  Bonus, not package-mandatory but directly tested: HABB immutability after grant (09 §50.1) and REVOKED-is-terminal (05 §8.1), both live-proven with real `RAISE EXCEPTION` messages.
- **CROSS_LAYER_TEST_RESULTS**: exercised against real predecessor layers — `semantic_types.{WorkspaceId, UserId, AuthorityBindingId, RecordVersion}` (PKG-00) and `persistence.workspace_repository`/`users`/`workspaces` tables (PKG-01) consumed directly; live PostgreSQL 17 container for every DB-backed test, no mocks
- **RECURSIVE_REGRESSION_RESULTS**: full suite (102 tests) re-run against an already-migrated database and again against a completely fresh database volume with `001` then `002` applied from scratch in this session — 100 passed / 2 skipped both times, identical result; PKG-00/PKG-01 tests unaffected; frontend lint/typecheck/test unaffected (unchanged, confirmed)
- **P_CLAIMS_TESTED**: P-10 (`tests/authority/test_current_authority.py`, "no current binding → DENY/no commit") — `introduced` only; full exercise needs `AuthorityResolver` (PKG-03), `BLOCKED_UPSTREAM`. P-11 (`tests/authority/test_stale_authority.py`, "revoke after prepare → BND-014 DENY") — `introduced` only; full exercise needs `CommitUnit`/BND-014 (PKG-13), `BLOCKED_UPSTREAM`; the REVOKED-terminal trigger this package adds is a genuine prerequisite for it. P-12 (`tests/governance/test_executable_binding.py`, "policy text/role only → no authority") — **exercised in substance** by `tests/authority/test_authority_binding_repository.py::test_owner_role_alone_yields_no_bindings`, which needs no `AuthorityResolver` to prove; the *designated* file `tests/governance/test_executable_binding.py` was not created here because `tests/governance/` is not on PKG-02's `FILES_ALLOWED_TO_CREATE` (only `tests/authority`/`tests/security` are) — left for whichever future package is authorized there.
- **PROOF_ARTIFACTS**: `git diff`/`git status` (below); live PostgreSQL 17 migration proof (upgrade/downgrade/re-upgrade round-trip, six independent constraint/trigger violations proven with real `RAISE EXCEPTION` messages); fresh-database-volume regression proof; full local CI-equivalent run (ruff/mypy/pytest/4 checker scripts)
- **FORBIDDEN_DEPENDENCY_CHECK**: `ARCHITECTURE_DEPENDENCY_CHECK::PASS` (0 violations, including after the disclosed `persistence → governance` extension)
- **PROVIDER_SDK_CHECK**: `PROVIDER_SDK_IMPORT_CHECK::PASS`
- **TEST_ONLY_IMPORT_CHECK**: `TEST_ONLY_IMPORT_CHECK::PASS`
- **DIFF_AUDIT**: 11 new files, 6 modified, 0 deleted (no out-of-band files this time — `.claude/settings.json` untouched). No new semantic type beyond the closed vocabularies 04 §9/05 §8/09 §23-24 already define; no transition; no authority *resolution* path (only structural persistence and data-integrity triggers); no DB write path in application code (repositories remain 100% read-only — all writes in this session are test-fixture-only raw SQL, exactly the PKG-01 precedent); no boundary weakened (none exists to weaken); no negative test removed; no admin shortcut (Owner-superuser explicitly disproven); no projection/cache truth; no AI canonical authority; no broader Workspace scope (every query explicitly `workspace_id`-filtered); migration `001` (PKG-01) untouched, `002` is new, not a change to existing migration semantics. The one deliberate dependency-table extension (`persistence → governance`, type-only) is disclosed with citation and proven via both positive and negative regression tests, added proactively in this same pass (the PKG-01 review found this gap after the fact; it is closed here as a standing practice, not just a one-off fix).
- **ARCHITECTURE_RECONSTRUCTION_RESULT**: `REQUEST → ACTOR → WORKSPACE → CURRENT STATE → CURRENT GOVERNANCE → CURRENT AUTHORITY → HUMAN DECISION → EVIDENCE → BOUNDARIES → BND-014 → COMMAND → COMMIT UNIT → CANONICAL MUTATION → AUDIT → OUTBOX → EVENT → RESULTING STATE`: PKG-02 extends PKG-01's reach again. `CURRENT GOVERNANCE` is now partially real: genuine (test-fixture) membership/role/HABB facts are durably persisted, structurally constrained (active-membership-required-for-grant, Workspace-scoped governance right, immutability, terminal revocation), and readable through typed repositories — not `SUCCESSOR_NOT_BUILT` for the *fact layer*, though the *resolution* layer above it (`CURRENT AUTHORITY` onward) remains fully `SUCCESSOR_NOT_BUILT`: no `AuthorityResolver`, `Human Decision`, `Boundary` evaluator, `BND-014`, `Command`, `CommitUnit`, `Audit`, `Outbox`, or `Event` exists. `RESULTING STATE` for this package's own operation is a raw governance fact row, read as a proven historical/current fact — never a governed canonical mutation, since `HARD-DEP-001` still blocks any legitimate grant (no bootstrapped governance root exists to be the grantor).
- **KNOWN_LIMITATIONS**:
  1. `facilitator_scope_bindings` was not created. 09 §51 requires a `session_id` FK, and `sessions` does not exist until PKG-05 (migration 003). 14 §9's one-line migration summary is imprecise about this real cross-table dependency; PKG-02's own `PUBLIC_INTERFACES` never requires a `FacilitatorScopeBindingRepository` either. Deferred to whichever migration introduces `sessions`, not stubbed.
  2. `AuthorityBindingRepository.list_current_bindings` deliberately does not cross-check `WorkspaceMembership` effectiveness (09 §51/05 AC-05-004's "stored ACTIVE alone does not prove effective authority"). This is the conservative, literal reading of "persist authority facts only, do not resolve them" — a future `AuthorityResolver` (PKG-03) must perform that join itself. Proven, not merely asserted, in `tests/authority/test_authority_binding_repository.py::test_stale_active_binding_is_not_silently_resolved_against_membership`.
  3. P-10 and P-11 remain `BLOCKED_UPSTREAM` for full exercise (need PKG-03 and PKG-13 respectively) — only `introduced` here.
  4. The sandbox Python-3.10-vs-3.13, npm/eslint/vitest-pin, and `POSTGRES_PORT` facts from PKG-00/PKG-01's reports still apply unchanged. One addition: `enum.StrEnum` (3.11+) is unavailable in this sandbox exactly like `datetime.UTC` was — `UP042` added to the ruff ignore list alongside `UP017`, same rationale.
- **BLOCKED_DEPENDENCIES**: `HARD-DEP-001` and `HARD-DEP-002` remain fully unresolved and untouched. No actual GRANT_MEMBERSHIP/GRANT_HUMAN_AUTHORITY_BINDING governance operation is implemented — all rows in this session's tests are test-fixture-only raw SQL inserts, explicitly not claimed as production-legitimate.
- **NEW_GAPS_DISCOVERED**: none new. `GAP-01-001` (Tenant vs Workspace), `GAP-04-005` (Facilitator Scope Binding — confirmed `RESOLVED` by 05 per `16_DECISION_GAP_REGISTER.md`, and its resolution correctly informs the `facilitator_scope_bindings` deferral above), and `GAP-05-001` (Workspace Root Creation/Succession, `HARD-DEP-001`'s architectural detail) are pre-existing, carried forward, not newly created or newly closed.
- **NO_SEMANTIC_INVENTION_CONFIRMATION**: confirmed. `AuthorityClass`'s 7 values are exactly 04 §9's Decision Right Classes — none added, none omitted. `WorkspaceRole`'s 5 values and casing are exactly 02/05's literal source vocabulary. The two database triggers encode preconditions 05 GOV-002/GOV-005 already state in prose (active membership, Workspace-scoped governance right, immutability, terminal revocation) — nothing beyond what those sections specify. No Decision Right, HumanAuthorityBinding *meaning*, AuthorityResolver fallback, or Owner/Admin superpower was invented; Owner-superuser was explicitly disproven, not implemented.
- **NEXT_PACKAGE_ALLOWED_BY_DAG**: PKG-03 (AuthorityResolver) is the DAG candidate once its predecessor (PKG-02) is verified complete — eligibility only, not authorization.
- **HUMAN_GATE_REQUIRED**: YES (Phase 1 gate, 14 §44 — same phase as PKG-01; PKG-03 begins Phase 1 too per its own manifest, to be confirmed when read)

## PACKAGE_VERDICT

`PACKAGE_PASS`

`NEXT_PACKAGE_ELIGIBLE`: PKG-03

`NEXT_PACKAGE_AUTHORIZED: NO`
