"""Provider adapters for the AI Gateway.

The ONLY location in this repository allowed to import a provider
SDK (14 §4, §41). `scripts/check_provider_sdk_imports.py` enforces
this boundary. Empty in this build phase (Phase 0); MockProviderAdapter
lands in PKG-19.
"""
